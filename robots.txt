User-agent: *
Allow: /

# NOTE: crawlers only read robots.txt at the origin root, and this site is a
# GitHub *project* site (https://danielswims101.github.io/JAC_Online/), so this
# file is never fetched while it lives here. The backend sources, maintenance
# scripts and repo docs are kept out of the published site by _config.yml
# (Jekyll `exclude`) instead, and sitemap.xml is submitted in Search Console by
# hand. This file becomes effective on a custom domain (tidelyne.com or whatever
# is bought): update the Sitemap line then.
Sitemap: https://danielswims101.github.io/JAC_Online/sitemap.xml
