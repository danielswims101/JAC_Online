# Tidelyne forensic audit log

Purpose: a permanent record of every finding raised by the forensic audit loop (one lens per auditor, one fixer per lens, one merger per round), with the fix that shipped and its status. Every finding is logged, including the ones that were deferred or could not be reproduced, so later rounds never re-discover the same issue without seeing what was decided. Findings JSON and the auditors' evidence live in the session scratchpad; the code is on main.

Column legend: **ID** — lens-scoped finding id (R<round>-<LENS>-<n>) · **Page** — hash route(s) or file the finding applies to · **Severity** — the auditor's rating (high / medium / low) · **Finding** — the auditor's one-line summary · **Fix applied** — what the fixer changed (and how the merger reconciled overlapping fixes) · **Status** — fixed / partial / deferred / not-reproduced.

## Round 1 — 2026-09-18

Round focus: first full sweep of the 3.1.1 site across six lenses — content & copy, visual & layout, function & data integrity, accessibility, code quality/performance/SEO, and swimming-domain accuracy — every page in the inventory, the nav/drawer/palette, the auth overlay, the legal pages, 404, sitemap, manifest and the service worker. Fixes were made on six branches (forensic/r1-*) and merged into main with conflicts resolved to keep every lens's intent; overlapping findings are cross-referenced in the Fix column. Post-merge smoke: check_index.py PASS, node --check on viz-human.js and sw.js, version strings untouched (3.1.1), all 22 pages open without console errors, the 3D lab builds and all five strokes render, drawer and palette open/close, backup v2 round-trips, calculators and log validation verified in the browser.

### Content & copy (copy) — 33 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R1-COPY-01 | race (Pacing Science tab) | high | Race page pacing rules contradict the site's own elite split data | Race › Pacing Science rewritten to match the Planner's PATTERNS (100: second 50 1.5–2.5 s slower, dive explained; 200: second 100 within ~2–3% for free/back, fly/breast fade more; 400+: even 100s with a lift) and linked to the Split Planner reference via the new showPageTab helper. Merged with R1-DOMAIN-05 into one paragraph (100 m 1.5–3 s fade by stroke, 200 m second 100 2–3 % / 2–3 s slower, hold the third 50; planner link kept). | fixed |
| R1-COPY-02 | technique (Underwater tab) | high | Technique page "Elite rate 180–210 kicks/min" for underwater dolphin kick is off by ~2× | Technique › Underwater 'Elite rate' now 'About 110–130 kicks/min at race speed (1.8–2.2 Hz). Rate, not amplitude, is what rises with speed.' — consistent with viz-human.js's 1.9 ± 0.3 Hz source and the 96 kicks/min release note. Merged with R1-DOMAIN-04: final copy reads "About 2.0–2.5 Hz (120–150 kicks/min) at full race speed; elite means sit near 1.9 Hz." | fixed |
| R1-COPY-03 | timers (Tempo Trainer) and analytics (Stroke rate & DPS) | medium | Timers stroke-rate table and Analytics elite-freestyle table disagree while citing the same study | Both stroke-rate tables now render from one top-level ELITE_RATES constant (renderEliteRateTables fills #tlt-preset-body and #an-ref-body); the Timers envelope is computed as min/max of the men's and women's bands (50 FR 56–67, 100 FR 46–55, 200 FR 38–50, 400 FR 38–53, 800/1500 35–55). Re-read the Staunton/Ruiz-Navarro 2025 paper: women's 400 47–53 is the winner's range, women's 800 field is bimodal ~39–40 / ~45–46 with the winner 48–55, so that row is now 39–55 with an explanatory bullet on Analytics and a note on Timers. Merged with R1-DOMAIN-18: ELITE_RATES breaststroke rows widened to 100 BR 44–58 and 200 BR 31–43 so the Timers table shows the domain lens's outward-rounded bands. | fixed |
| R1-COPY-04 | technique (IM tab) vs planner (Split Planner reference ta… | medium | 200 IM leg percentages differ between Technique and Planner | Technique › IM elite split now fly ~22 / back ~25 / breast ~29 / free ~24% of race time, with a link to the Planner's pattern table. Merged with R1-DOMAIN-06: final text "fly ~22%, back ~25%, breast ~28–29%, free ~24–25%" (matches PATTERNS 21.9/25.0/28.6/24.5). | fixed |
| R1-COPY-05 | zones, taper | medium | "Golf scale" used as the name of the heart-rate 10-second count | 'Golf scale' replaced with 'on the 10-second count' on Zones (Z2, Z3) and Taper (active recovery); the workout-generator system prompt now asks for HR as a 10-second pulse count so the AI stops emitting 'Golf scale' too. Merged with R1-DOMAIN-07 (wording "on the 10-second count" kept site-wide; Zone 4 card gained HR 30–33). | fixed |
| R1-COPY-06 | privacy.html. Anchor `Your training log entries and the w… | medium | Privacy policy §2 under-reports what is stored on the device | privacy.html §2 gained a bullet for Race Planner goals/meets, timer settings, analytics preferences, recent searches and the last-read release; 'Last updated' bumped to September 18, 2026 (material disclosure change). | fixed |
| R1-COPY-07 | planner, profile | medium | Planner data is not in the backup, and the Planner never says so | Backup schema v2: exportBackup writes planner:{tl_goals, tl_meet, tl_pred_fit}; importBackup restores them (whitelisted keys, must parse as JSON, length-capped) and still accepts v1 files; My PRs copy, Planner intro, import alert and README updated. Verified export/import round trip in the browser. Merged with R1-FUNCTION-08: the function lens's v2 schema (tl block, 8 allow-listed keys, validated) is the one shipped; the planner intro and My PRs copy say the backup covers planner goals/meets and timer/analytics settings. | fixed |
| R1-COPY-08 | home | medium | Home metric card "6 New Sections — Race, Nutrition, Mental…" is stale | Home metric card is now '3 · Deck Tools · Timers, Planner, Analytics'; hero description mentions pool-deck timers, race planner and training analytics. Merged with R1-DOMAIN-27 (same card text). | fixed |
| R1-COPY-09 | profile → calculator | medium | "Calculate Pace Zones →" on My PRs lands on the Golf Score tab | Added showPageTab(page, tabId) helper (finds the tab button beside the panel); 'Calculate Pace Zones →' now lands on the Pace Zones tab (verified: hash #calculator, active panel c-pace). | fixed |
| R1-COPY-10 | analytics, disclaimer, privacy, terms, updates | medium | Mixed British and American spelling in user-facing copy | User-facing copy normalized to US spelling: index.html (meters ×6, ANALYZE, programs ×4, reorganized, colors) plus disclaimer.html (program ×2, practice, fueling), privacy.html (honor), terms.html (licenses). Code-only comments left untouched. | fixed |
| R1-COPY-11 | home footer | medium | Home footer navigation omits half the tool pages | Home footer gained a Tools column (Pace Calculator, Timers, Race Planner, Training Analytics), Injury Prevention and Season Calendar under Compete, and a What's New link in the bottom bar. | fixed |
| R1-COPY-12 | nutrition (Daily Fueling vs Recovery tabs) | medium | Nutrition recovery numbers contradict each other | Post-Session card now '~1 g/kg carbs (60–80g for most swimmers) + 20–30g protein'; carbohydrate targets say 'Race day: 8–10 g/kg on the day before (start 2 days out for multi-day meets)'. | fixed |
| R1-COPY-13 | race (Warm-Up tab) | medium | Warm-up protocol adds up to ~1,130–1,330 m but the box says 1,200–1,800 m | Warm-up box now 'Total: about 1,200–1,400m. Anything past ~1,800m is a training set before your race.' | fixed |
| R1-COPY-14 | privacy.html. Anchor `only the AI coach features need an … | medium | Privacy §6 says only the AI coach needs an account; three other features do too | privacy.html §6 now names all four account-gated AI features (coach, workout generator, visualizer AI analysis, AI 3D reconstruction). | fixed |
| R1-COPY-15 | sitemap.xml | low | Sitemap lastmod dates are stale | sitemap.xml lastmod: root 2026-09-16 (3.1.1), privacy/terms/disclaimer 2026-09-18 (edited today); README release checklist gained a step 4 for sitemap lastmod. Merged with R1-CODE-17: all four lastmod values are 2026-09-18, the date this round's edits land in every file. | fixed |
| R1-COPY-16 | index.html | low | Six comments point to a NOTES.md that does not exist; sitemap comment names a non-existent #tempo page | All six NOTES.md references repointed to real sources (USA Swimming calendar, LC world records, the Split Planner caption, README 'Releasing a new version') — no NOTES.md fabricated; sitemap comment '#tempo' → '#timers'. Merged with R1-CODE-11 (comments point at README "Maintainer notes" and the named public sources). | fixed |
| R1-COPY-17 | index.html | low | Stale build-pipeline comments shipped in production markup/JS | Deleted the PATCH BUNDLE comment; rewrote the script.js / nav_button.html comments (CSS 1024, 1133, 1197, 1215, 1222, 1224, 1269 and the nav markup comment) as plain descriptions of the current code. | fixed |
| R1-COPY-18 | index.html | low | Three different taglines for the site in metadata | og:title and twitter:title now use the <title> tagline; site.webmanifest description aligned with it. | fixed |
| R1-COPY-19 | index.html | low | Zone-1 heart-rate ceiling is 23 on the Zones page and 22 everywhere else | Zones page Zone-1 ceiling is now 'HR <22' in the bar and 'under 22' in the card, matching Sets/Race/Taper. | fixed |
| R1-COPY-20 | index.html | low | Program-internal jargon and inconsistent meso naming on Sets/Zones/Technique | 'Meso 2' → 'Meso II'; 'DB check Rd 2' → 'Benchmark check on Rd 2'; 'Rd 1 with Duck' → 'Rd 1 with the Duck drill (Technique › Backstroke)'; 'The program features UW 15m' → 'Elite programs feature UW 15m work'; Zone-3 'First appears Week 4' → 'Typically introduced around Week 4 of a season'. | fixed |
| R1-COPY-21 | index.html | low | Rate-limit messages worded three different ways | One top-level AI_LIMIT_MSG constant used by the coach, generator, visualizer analysis and 3D reconstruction 429 paths. | fixed |
| R1-COPY-22 | profile | low | "Clear All PRs" renders as "CLEAR ALL PRS" | Button now 'Clear All PR<span style="text-transform:none">s</span>' so it renders CLEAR ALL PRs. | fixed |
| R1-COPY-23 | season | low | Season heading has the comma inside the coloured span and ", &" | Season heading markup is 'FUTURES, JUNIORS <span>& NATIONAL CUTS</span>' (verified rendered text). Merged with R1-VISUAL-22 (final markup "FUTURES, JUNIORS <span>& NATIONAL CUTS</span>" as an h2). | fixed |
| R1-COPY-24 | timers (Pace Clock) | low | Pace-clock minute hand is "black" in the copy and "grey" in the SVG title | SVG title and info box both say the minute hand is black on deck clocks and drawn grey here. | fixed |
| R1-COPY-25 | index.html | low | Over-claims in AI copy: "trained on …", "Always online" | Home AI card says 'briefed on race strategy…'; coach header is 'Expert coaching knowledge · Streaming · Free account'. | fixed |
| R1-COPY-26 | index.html | low | What's New 3.0.0 "Installable app" item links to the page it is on | Dropped the self-referential link:'#updates' from the 3.0.0 'Installable app' item (text, date and version untouched). | fixed |
| R1-COPY-27 | analytics | low | Analytics footer cites "the project notes", which readers cannot see | Analytics footer now names the sources on the page (Seiler 2010; Hellard et al. 2019; Wakayoshi et al. 1992; Dekerle et al. 2005; Staunton, Ruiz-Navarro & Born 2025). | fixed |
| R1-COPY-28 | log vs analytics | low | Log page assumes metres ("Distance (m)", "Km Swum") while Analytics says yards are fine | Log label 'Distance (m or yd)' with placeholder 'e.g. 4500 — use one unit'; summary stat 'K swum (m or yd)'; Analytics hint reworded to match. | fixed |
| R1-COPY-29 | taper | low | Taper "Recovery Modalities" tab duplicates the Sleep tab | Recovery-tab SLEEP card reduced to one line plus a link that switches to the Sleep tab (verified); the hours guidance moved into the Sleep tab so nothing is lost. | fixed |
| R1-COPY-30 | README.md | low | README omits the supabase/ folder and repeats the local-preview block | README: supabase/ row added, duplicate preview block merged into the first, Data & privacy line includes planner goals, release checklist gained the sitemap step. | fixed |
| R1-COPY-31 | technique (Starts) | low | "GRAB START & TRACK START MECHANICS" never describes the grab start | Starts accordion now describes the grab start in one sentence (both feet forward, hands gripping the block; stronger but slower push, rare at elite level). Merged with R1-DOMAIN-26 (one sentence covers the feet/hands position, the slower push and the relay take-over use). | fixed |
| R1-COPY-32 | disclaimer.html | low | Disclaimer names an eating-disorder helpline without a link | disclaimer.html links 'National Alliance for Eating Disorders' to allianceforeatingdisorders.com (rel=noopener); helpline number left for the owner to confirm. | fixed |
| R1-COPY-33 | visualizer at 375 px | low | Mobile: visualizer "Ask the Visualizer" form grid overflows its card by 16 px | .imggen-grid tracks are minmax(0,1fr) and .imggen-controls / its select and textarea have min-width:0 — at 375 px the grid scrollWidth now equals clientWidth (269 = 269), was 285 > 269. | fixed |

### Visual & layout (visual) — 27 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R1-VISUAL-01 | auth overlay (any page) | high | Sign-up card is clipped and unreachable on short phones | Reproduced at 375x667 (signup card y=-38, gate overflow visible). Moved the gate's layout out of the inline style into CSS: #access-gate{align-items:flex-start;overflow-y:auto;overscroll-behavior:contain;padding:2rem}, the 440px wrapper gets margin:auto (centred when it fits, scrolls otherwise); the three screens now use .auth-card (2.75rem 2.25rem, 2rem 1.25rem at <=480) and the gate padding drops to 1rem 0.75rem on phones. After: card top y=16, gate scrolls 88px to reveal the bottom, inputs 309px wide. | fixed |
| R1-VISUAL-02 | technique (desktop), all tabbed pages (phone) | medium | Tab strips are silently clipped (desktop and phone) — hidden scrollbar, no affordance | Reproduced (technique strip 909px in 896px at 1280; 2-3 screens of hidden tabs at 375). >=900px .tabs wrap (after: 896/896, two rows). Below that the strip keeps scrolling with scroll-snap and .tab-btn snap-align, and enhanceA11y() toggles .tl-scroll-more (right-edge mask fade) on .tabs/.ai-mode-btns/.quick-prompts/.viz-phase-bar via scroll+resize+hashchange+ResizeObserver; verified the class clears at scrollLeft end. .tab-btn padding 11px 13px at <=768. Merged with R1-A11Y-13: .tabs use the a11y lens's two-edge CSS-variable mask; the .tl-scroll-more right-edge fade covers the AI mode chips, quick prompts and phase bar. | fixed |
| R1-VISUAL-03 | technique/…(accordions), home (feature cards), nav brand,… | medium | No visible keyboard focus ring on the custom controls | Reproduced (outline none on focused .acc-header/.card[onclick]/.nav-brand). Extended the focus-visible rule with [role="button"], [role="link"] and #viz3d-container, plus an inset -3px offset for .acc-header/#viz3d-container/.phase-step whose overflow:hidden parents would clip an outside ring. Verified the rules are live in the stylesheet and the elements carry the roles (the hidden pane cannot take real key input, so no keyboard screenshot). Merged with R1-A11Y-14: one focus-visible rule covers buttons, links, inputs, [role=button], [role=link], [tabindex]:not(.page) and #viz3d-container, with the inset variant for clipped parents. | fixed |
| R1-VISUAL-04 | visualizer | medium | 3D lab phase bar: 11 px-tall hit areas, 8.3 px text, not focusable | Reproduced (steps h:11, 8.3px, <div> without role). Bar now align-items:stretch; steps are <button type=button aria-pressed> (reset styles, flex-centred, 0.6rem, hover tint), updatePhaseBar3D keeps aria-pressed in sync; 768 size 0.56rem, 480 size 0.6rem/min-width 74px with the scrollbar hidden and snap. After: BUTTON 39px tall at 1280, 35px at 375. Merged with R1-A11Y-09: buttons with aria-pressed, 0.62rem in --white3, stretch-aligned, snap-scrolling at ≤480. | fixed |
| R1-VISUAL-05 | log, profile, workouts, calculator, analytics, visualizer… | medium | 14 px form controls make iOS Safari zoom the page on focus | Reproduced (14px / 14.08px at 375). Added .form-input,.form-select,.form-textarea,.ai-input{font-size:16px} to the <=768 touch block. After: 16px on log inputs/selects, AI input and auth inputs. | fixed |
| R1-VISUAL-06 | home | medium | Home feature-card grid has ~200 px of empty space in one card | Reproduced (row 1 447px with the Technique card holding 243px of content). Gave the four short cards a 3-4 item list drawn from their actual page tabs (technique: strokes/underwaters-turns-starts/drills/cues; race: meet-day+warm-up/event plans/splits+pacing; nutrition: daily/race-day/recovery+hydration; mental: visualization/arousal/focus+routine). After: rows 447/387, the blank area in card 3 is ~26px. | fixed |
| R1-VISUAL-07 | auth overlay | medium | Auth overlay close button is a 20×26 px target | Reproduced (20x26). Close button is now a 44x44 inline-flex, position:fixed (stays visible while the gate scrolls), type=button, radius 8px. After: 44x44 at (323,8) on phones. Merged with R1-A11Y-21 (fixed-position 44×44 button with aria-label "Close sign-in dialog"). | fixed |
| R1-VISUAL-08 | auth overlay | medium | Auth screen links are <a> without href: unfocusable, 17 px tall, wrap mid-phrase | Reproduced (no href/tabindex, 17px, mid-phrase wrap). Replaced the four <a onclick> with <button type=button class=tl-linkbtn> (44px min-height, cyan, hover underline) inside .auth-links flex rows; dropped the nbsp-dot separator; showAuthScreen now moves focus into the newly shown screen's first input so Enter/Space on a link keeps the keyboard user inside the dialog. After: buttons 44px tall, tabIndex 0, focus lands on signup-username after 'Create an account'. | fixed |
| R1-VISUAL-09 | all (nav) | medium | Phone nav gutter is 5 px — logo and burger hug the screen edges | Reproduced (padding 0 5px, brand x=5, burger right 370). nav.tl-pal-nav is now padding:0 12px;gap:6px at <=480. After: brand x=12, burger right 363, document width 375 (no overflow). | fixed |
| R1-VISUAL-10 | home/season (card-tag purple), visualizer ("✦ AI Reconstr… | medium | Muted text and purple accents fall under 4.5:1 | Reproduced (card-tag purple 4.29:1, #motion-btn 4.22:1, planner dd.muted 4.46:1). --white4 is now #7b95ad and a --purple-text:#bf8cff token replaces var(--purple) on the three purple .card-tag labels and #motion-btn. After: 6.8 / 6.68 / 5.36. Merged with R1-A11Y-15/16: final tokens --white4 #7b95ad and --purple-text #bf8cff (defined once on :root). | fixed |
| R1-VISUAL-11 | ai | medium | AI Coach header eats 258 px on phones before the chat | Reproduced (header 258px, input row at y=1448 — the 519px quick-prompt block was the larger cause). <=480: header padding 0.9rem 1rem, mode chips a single swipeable row (flex 1 1 100%, nowrap, hidden scrollbar, edge fade), quick prompts the same treatment, .ai-messages calc(100dvh - 300px). Clear stays as the last chip (one control rather than a duplicate). After: header 139px, quick prompts 64px, input row at y=895. | fixed |
| R1-VISUAL-12 | season | low | Season meet table overflows the phone by 10 px | Reproduced (343 vs 333). <=480 .data-table cells pad 9px 10px and the Dates column may wrap. After: 333/333. | fixed |
| R1-VISUAL-13 | home | low | Five metric cards never fill their grid (orphan tile) | Reproduced (768: 4+1 metrics, footer 4 in 3, dl 5 in 2, nutr 3 in 2, key 4 in 3; 375: 2+2+1 with a taller orphan). #page-home .metrics-row uses a 6-track grid at <=768 (span 2 / span 3 -> 3+2 full rows) and at <=480 a 2-col grid with the odd last card spanning; .footer-inner 2 cols <=899 / 1 col <=480; .key-grid 2 cols <=899 / 1 col <=480; .nutr-grid 2 cols <=899 with an odd last card spanning, 1 col <=600; .dl-grid odd last card spans at <=768. Verified at 768 (3+2, footer 2, dl 2+2+full, nutr 2+full, key 2+2) and 375 (2+2+full). | fixed |
| R1-VISUAL-14 | visualizer | low | 3D lab control buttons have four different heights | Reproduced (33/29/33/30). .viz-btn is inline-flex, min-height 34px, gap 6px, line-height 1.2. After: 34px on every row (the AI-reconstruct row is 40px by design, stretched to its input). | fixed |
| R1-VISUAL-15 | all (nav) | low | Nav right-cluster buttons are misaligned by 1–4 px | Reproduced (28/25/27 at 1280; 44 vs 40 at 768). Desktop (>=769) .nav-right>button min-height 30px inline-flex; the touch block now gives .nav-ai-btn/.nav-key-btn/login/logout min-height 44px like the palette button and burger. After: 30/30/30 at y=13 on desktop; 44/44/44/44 at 768 and 375. | fixed |
| R1-VISUAL-16 | all (nav ≥900) | low | Desktop nav links are 58 px tall inside the 56 px bar | Reproduced (#nav-home y=-1 h=58). line-height 48px -> 46px on the top-level nav links/group buttons. After: y=0 h=56, underline flush with the 56px bar. | fixed |
| R1-VISUAL-17 | home | low | Phone hero: 1030 px tall, three ragged CTA pills, uneven stat pairs | Reproduced (hero 1030px, CTAs 221/177/205, stats 134/109/67/67). <=480: hero padding 3.5rem 1.25rem 3rem with min-height auto, CTAs stack full-width (max 340px), stats a 2x2 grid. After: hero 875px, H1 at y=167, CTAs 335px, stats 160px x4 in two even rows. | fixed |
| R1-VISUAL-18 | visualizer | low | Lab physics row: 6 boxes in 5 tracks (Efficiency alone on row 2) | Reproduced (5 tracks for 6 boxes). minmax(130px,1fr). After: 6 tracks. | fixed |
| R1-VISUAL-19 | log, workouts, visualizer (analysis form), profile | low | Phone forms waste 49 px per side; PR grid collapses to one column | Reproduced (log inputs x=49 w=277; PR grid 1 column). <=480 .gen-form/.gen-output/.imggen-wrap/.calc-panel pad 1.1rem, and the three inline-styled panels (log form, workout page card, season-now card) got a .tl-panel class with the same phone padding; .pr-grid minmax(150px). After: log inputs x=39 w=298, PR grid 2 columns (161px cards). | fixed |
| R1-VISUAL-20 | planner, visualizer, timers | low | Sub-9 px labels (0.5–0.52rem) | Reproduced (8px / 8.32px). Floored .vos-lbl, .tlt-th-sub, .pl-halves .k, .pl-goal-stats dt, .pl-cp-when .pl-tag (and .phase-step) at 0.6rem. After: 9.6px; the overlay stats box grew 172->187px and still fits the 333px canvas. | fixed |
| R1-VISUAL-21 | visualizer | low | Info panel shows "Select a stroke" while freestyle is already playing; stroke label falls back to the raw id | Reproduced ('Select a stroke' / '' on load). Added STROKE_LABELS + strokeLabel3D() and seedInfoPanel3D(), called from startViz3D, setStroke3D and loadCustomMotion, which fills phase name/desc/stroke label and the bar synchronously; syncInfoPanel3D uses the label helper. After: 'Phase 1: Entry & Extension' / 'Freestyle' on load, 'Dolphin Kick' after switching. | fixed |
| R1-VISUAL-22 | season | low | Cyan comma in "FUTURES, JUNIORS, & NATIONAL CUTS" | Reproduced in the markup. Now 'FUTURES, JUNIORS, <span>& NATIONAL CUTS</span>'. Merged with R1-COPY-23. | fixed |
| R1-VISUAL-23 | home (metric cards), season, taper, mental, … | low | Non-interactive cards lift on hover like buttons | Reproduced (all .card/.metric-card lift on hover). Plain cards keep only the border-colour change; the lift/shadow applies to .card[onclick]/.card[role=button] and .metric-card[onclick] inside @media(hover:hover), so touch taps no longer leave a stuck lift. | fixed |
| R1-VISUAL-24 | 404.html (also privacy/terms/disclaimer) | low | 404 page keeps the retired low-contrast grey; static pages are off-brand | 404 .small is #8ba5bd (was #607a92 ~3.1:1); 404, privacy, terms and disclaimer load the same Google Fonts stylesheet the app uses and set DM Sans body / Bebas Neue h1 so the hand-off matches the app. Merged with R1-A11Y-22 (same colour) and R1-CODE-16 (legal-page head meta). | fixed |
| R1-VISUAL-25 | all | low | Footer legal links are 9.6 px text with 13 px-tall targets on every page | Reproduced (9.6px, 54x13 targets; column links 17px). .footer-bottom a is inline-block 0.7rem with 10px 4px padding and white3 colour (cyan hover); .footer-col links are inline-block with 5px vertical padding. After: legal links 11.2px in 38px targets, column links 31px. | fixed |
| R1-VISUAL-26 | all (nav) | low | Empty username span leaves an 8 px spacer in the nav | Reproduced (8x0 spacer at 1280). #current-username-display:empty{display:none}. After: display none while logged out. | fixed |
| R1-VISUAL-27 | visualizer | low | Motion-prompt placeholder is 565 px of text in a 335 px phone input | Reproduced (88 chars, 565px in a 333px input). Placeholder is now '✦ Describe a movement, e.g. "flip turn"' (282px vs 309px inner width — the first 50-char attempt still measured 374px) and the two longer examples live in the #motion-status helper line, which generateMotion overwrites as before. | fixed |

### Function & data (function) — 22 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R1-FUNCTION-01 | My PRs (Import Backup) → affects every page | high | Importing a backup with a non-object log entry breaks the whole app on every later load | readLog() now drops non-object entries, normalises ids to numbers and writes the repaired array back; importBackup routes the log through a new sanitizeLogEntries() (plain objects with a real YYYY-MM-DD date, coerced numeric fields, capped text, ids assigned); the top-level initProfile() and renderLog() calls are wrapped in try/catch so stored data can never abort the app script. Verified: with swimiq_log='[null]' the page now loads with initTimers/initPlanner/initAnalytics/openPalette defined, nav bundle loaded, What's New populated, and the stored log repaired to []. | fixed |
| R1-FUNCTION-02 | Training Log (data from Import Backup) | medium | Log entries from a backup file are injected into HTML/onclick without escaping (self-XSS) | renderLog coerces dist/dur/effort/id with Number() before interpolating, and the delete button is a data-del-id attribute handled by one delegated click listener on #log-entries (no inline onclick built from stored ids). Verified: the crafted entry renders as text (window.__pwned/__pwned2 stay undefined), and the delegated delete removes the entry. Merged with R1-A11Y-06 and R1-CODE-24: one .log-del button with data-del-id (delegated click), aria-label "Delete session <date> <type>" and CSS hover/focus colouring. | fixed |
| R1-FUNCTION-03 | Calculator › Race Splits | medium | "Surge Middle" split strategy does not add up to the target time | calcSplits builds a zero-mean offsets array for every strategy, so the cumulative column always equals the target. Verified: 200 @ 2:00 surge and 1500 @ 16:00 surge both end at exactly the target. Merged with R1-DOMAIN-03: the calculator now uses the planner's buildSplits (single source of truth) with a renormalised surge pattern; verified every strategy sums exactly to the target for 100/200/1500. | fixed |
| R1-FUNCTION-04 | Calculator › Race Splits | medium | Negative/positive split ramp is a fixed 0.4 s per 50 regardless of distance, giving impossible splits for long events | Negative/positive ramps are 4% of the average 50 from first to last (a 200 keeps its 1.2 s spread) and every 50 is clamped to >= 15 s. Verified: 1500 @ 16:00 negative split is now 32.64 -> 31.36 instead of 37.80 -> 26.20. Merged with R1-DOMAIN-03: the ±2 % half-difference ramp from buildSplits replaces the 4 % ramp (1500 @ 16:00 negative → 31.27 … 32.6 s per 50). | fixed |
| R1-FUNCTION-05 | every page (skip link at top of body) | medium | "Skip to content" link sends the user to the Home page | activatePage's guard now recognises an in-page anchor (the skip link's #main): it focuses the element with tabindex=-1 and restores the current view's hash via history.replaceState instead of bouncing to Home. Verified on #taper: after activating the skip link the hash is #taper, page-taper stays active, focus is on #main. | fixed |
| R1-FUNCTION-06 | Training Log / Analytics | medium | Training log silently discards the oldest session after 100 entries | Log cap raised from 100 to LOG_MAX = 2000 (save and import), and the Training Log intro states the limit and points to Export Backup. | fixed |
| R1-FUNCTION-07 | Training Log | medium | Log form accepts negative distance, absurd duration and effort outside 1–10 | saveLogEntry validates date, distance (0-100,000 m), duration (0-600 min) and effort (blank or an integer 1-10) with an inline role=alert message (#log-form-err), aria-invalid + focus on the offending field; the inputs carry min/max/step; sanitizeLogEntries applies the same caps on import. Verified: -500 / 99999 / 99 each produce the right message and nothing is saved until corrected. Merged with R1-CODE-23: the validation messages go through tlNotice("log-notice") (the separate #log-form-err element was dropped) and the success notice reads "Session saved — N logged." | fixed |
| R1-FUNCTION-08 | My PRs (backup) — affects Planner, Timers, Analytics | medium | Export/Import Backup does not include planner, timer or analytics data | exportBackup is version 2 and includes a validated tl object with tl_goals, tl_meet, tl_pred_fit, tl_split_last, tl_tempo, tl_interval, tl_paceclock and tl_an_prefs; importBackup restores those keys (plain objects only, allowlisted), keeps v1 files importable and reloads once so the timer/planner modules pick the settings up. Verified: v2 import restored 3 settings (bogus key and array value ignored), tempo showed 72 spm after reload, export contained the tl block. Merged with R1-COPY-07 (same schema; import feedback now goes through tlNotice and reloads after 1.5 s when settings were restored). | fixed |
| R1-FUNCTION-09 | My PRs | medium | PR fields accept any text with no validation or feedback | savePR parses the value with parseTime: invalid text is not stored, the input gets aria-invalid (red border) and a shared #pr-hint status shows 'Use ss.ss or m:ss.ss'; on change a valid value is normalised (52.3 -> 52.30); clearing the field removes the key. Verified for 'fast', '1:75', '52.3' and blank. | fixed |
| R1-FUNCTION-10 | Planner › Race Predictor | medium | Race predictor's "fit your own exponent" accepts physically impossible pairs | runFit rejects fitted exponents below 1.0 ('Your longer swim is faster per 100 than your shorter one...') and above 1.4 before saving. Verified: 52.30 + 1:30.00 is rejected and tl_pred_fit stays empty; 52.30 + 1:55.00 fits (b = 1.137); 4:00.00 is rejected as implausible. | fixed |
| R1-FUNCTION-11 | My PRs | low | Import accepts files that are not Tidelyne backups and reports success | importBackup requires a plain object with app === 'Tidelyne' and a numeric version >= 1, accepts only pr_ keys that map to PR_EVENTS with parseTime-valid values, and reports restored/skipped counts. Verified: [1,2,3] and {app:'Other'} are rejected; pr_evil and a numeric pr_50_FR are skipped while pr_100_FR '52.30' is restored ('1 PR, 1 session restored. 6 unreadable items were skipped.'). | fixed |
| R1-FUNCTION-12 | Calculator › Pace Zones (and everywhere parseTime is used) | low | parseTime accepts "1.55" as 1.55 s (contradicting its own comment) and Pace Zones has no plausibility check | parseTime now rejects a bare decimal under 10 s (so '1.55' is a typo, matching its comment); calcPaceZones requires 35 s-4:00 and, for an input shaped like m.ss, suggests the m:ss form. Verified: parseTime('1.55') === null, '5' and '20' alert with clear messages, '1:05.5' renders the table. Merged with R1-CODE-23 (messages via tlNotice("pace-notice")). | fixed |
| R1-FUNCTION-13 | router | low | Unknown hashes stay in the address bar and history | Unknown hashes are cleaned from the address bar with history.replaceState before Home is shown. Verified: '#nonexistent' -> page-home with location.hash ''. | fixed |
| R1-FUNCTION-14 | Calculator, Timers (split taker) | low | Time formatters print "60:00" instead of "1:00:00" at one hour | formatTime and fmtSW gained an hours branch. Verified: formatTime(3600) = '1:00:00.00', fmtSW(3599.99,1) = '1:00:00.0'. | fixed |
| R1-FUNCTION-15 | Planner › Split Planner | low | Split planner keeps the previous table on screen when a new run errors | runSplits uses a fail() helper on every error path that resets #pl-sp-out to the placeholder, hides the Copy button, clears the comparison and lastSplit. Verified: a valid 500 y plan followed by a 0:30 goal leaves 0 rows, the placeholder text, Copy hidden and an empty comparison. | fixed |
| R1-FUNCTION-16 | 3D Visualizer | low | 3D lab view buttons never indicate the current view | View buttons carry data-view and aria-pressed; setView3D marks the current one (markView3D) and a manual orbit/zoom (mousedown/touchstart/wheel on the canvas while controls are enabled) clears it. Verified in the 3D lab. | fixed |
| R1-FUNCTION-17 | Timers › Interval Timer | low | Interval timer cues are scheduled only inside the rAF frame, so a backgrounded/locked screen never beeps | While running, the interval timer also places cues from a 250 ms setInterval (itTick) with a 1.5 s lookahead when the document is hidden, finishes the circuit, and renders the display; keyed cues never double-fire. Verified with the pane hidden: 3 s after Start the display read 'GET READY / 7 / Elapsed 0:03' instead of the stale 'READY'. | fixed |
| R1-FUNCTION-18 | 404.html | low | 404.html redirects with a relative path, which fails for nested URLs on GitHub Pages | 404.html derives the site root from the first path segment (/JAC_Online/ on github.io, / on a custom domain) for the redirect, the button and the favicon, with a comment for a future rename. Verified on the local server (root '/'). Merged with R1-CODE-01 and R1-A11Y-22: 404.html derives the root from the hostname (project folder on *.github.io), waits 20 s and offers "Stay on this page". | fixed |
| R1-FUNCTION-19 | Videos, 3D Visualizer demos | low | Console warning "Unrecognized feature: 'web-share'" every time a video is played | web-share removed from the youtube-nocookie iframe allow list. | fixed |
| R1-FUNCTION-20 | AI Coach | low | AI Coach has no nav item, so nothing in the nav is marked current on #ai | The AI Coach button has id nav-ai-coach-btn; activatePage toggles .active and aria-current='page' on it for #ai (styled with a cyan ring). Verified on #ai and cleared on #home. | fixed |
| R1-FUNCTION-21 | Workout Generator | low | Workout generator notes field is unbounded and pasted straight into the prompt | gen-notes has maxlength=300 and the value is trimmed and sliced to 300 before it enters the prompt. | fixed |
| R1-FUNCTION-22 | What's New (update toast) / offline | low | Service worker: "previous version" comes from unsorted cache keys, and query-string navigations are cached as separate entries | sw.js sorts stale cache versions with a numeric compareVersions before choosing 'previous', and networkFirst stores page responses under origin+pathname (pageKey) so query-string navigations share one entry. Verified with node (sort order, key normalisation, node --check); the pane cannot register service workers. Merged with R1-CODE-07/08/09: shellKey() keys shell navigations under INDEX and every other page by origin+pathname; cmpVer picks the newest stale cache. | fixed |

### Accessibility (a11y) — 25 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R1-A11Y-01 | technique, videos, sets, dryland, visualizer, workouts, c… | high | 20 of 22 views have no page heading; titles are <div class="section-title"> | All 23 <div class="section-title"> page titles are now real headings: the first on each view is <h1 class="section-title"> (h2 on home, where the hero h1 already exists; also h2 for the second titles on visualizer/season). .section-title pins font-weight:400 so the switch from div to h1/h2 keeps the exact Bebas Neue rendering (zones' h1 was the only one rendering synthetic bold; it now matches). enhanceA11y additionally exposes the styled h3/h4 blocks at the next level down via role=heading/aria-level (rank compression per view, footer excluded) so every view reads h1 > h2 > h3 with no skipped level; verified in the browser: 22/22 views start at h1, 0 skipped levels. Merged with R1-CODE-05 (identical change; every view now has exactly one h1). | fixed |
| R1-A11Y-02 | home | high | Home feature cards are role=button with a 343-character name and headings swallowed | Home feature cards are plain containers (.card.card-clickable, position:relative) with a real <a href="#page" class="card-link"> inside the <h3>; .card-link::after covers the card so the whole card is still clickable (inset 0 verified), :has(.card-link:focus-visible) draws the site ring around the card. Removed the inline onclick/cursor styles and dropped .card[onclick] from enhanceA11y; the 6 h3s are back in the heading tree, 0 role=button cards remain, link name = title only. | fixed |
| R1-A11Y-03 | profile (My PRs) | high | 18 PR inputs have no label (placeholder "—" only) | buildPRGrid renders <label class="pr-event" for="pr-in-{key}"> + <input id="pr-in-{key}" aria-describedby="pr-format-hint">; one sr-only hint on the profile page explains the m:ss.hh format. pr_* storage keys untouched. Verified: 0 unlabeled PR inputs (was 18), label '50 FR' resolves. | fixed |
| R1-A11Y-04 | technique, videos, sets, visualizer, calculator, nutritio… | high | Tabs: role=tab without aria-controls, no role=tabpanel, no roving tabindex, unnamed tablists | enhanceA11y now implements the full tabs pattern: tablist aria-label from the view's .section-title (or data-label), each tab gets id/aria-controls parsed from its switchTab onclick, each .tab-panel gets role=tabpanel + aria-labelledby (tabindex=0 only when it has no focusable content), roving tabindex 0/-1 (also maintained in switchTab, including the planner's programmatic switch), plus Home/End keys. Verified: 63/63 tabs wired, 63 labelled tabpanels, 13/13 tablists named, ArrowRight/End/Home move focus and selection. | fixed |
| R1-A11Y-05 | technique (16), dryland, calculator, nutrition, race, tap… | medium | Accordion headers: <h4> nested inside role=button, no aria-controls | enhanceA11y moves each accordion title into a real <button type=button class=acc-btn aria-expanded aria-controls=acc-body-N> inside the h4 (font/letter-spacing inherited, so it renders identically); the header div no longer gets role=button/tabindex, and toggleAcc resolves closest('.acc-header') and updates aria-expanded on the button. Clicks still bubble to the header's onclick, so the palette's openAcc path works unchanged. Verified: 18 buttons with matching bodies, 0 headings inside buttons, toggling syncs aria-expanded. | fixed |
| R1-A11Y-06 | log | medium | Log-entry delete button has no accessible name and is 21×30 px | Log delete is <button type=button class=log-del aria-label="Delete session {date} {type}"> with the glyph aria-hidden; .log-del gives a 44x44 target and the red state on :hover and :focus-visible (inline onmouseover/onmouseout removed). Verified name 'Delete session 2026-09-10 Aerobic', min-height 44px. Merged with R1-FUNCTION-02 / R1-CODE-24. | fixed |
| R1-A11Y-07 | all (Log In / Sign Up dialog) | medium | Auth-gate error messages are never announced | login-error/signup-error carry role=alert, forgot-msg role=status aria-live=polite; showAuthError marks that screen's inputs aria-invalid=true + aria-describedby=<message id>, and showAuthScreen clears both. Verified live: after showAuthError('login') the email field has aria-invalid/aria-describedby=login-error, cleared on screen change. | fixed |
| R1-A11Y-08 | home (any deep link) | medium | On first load focus is moved to the page body, so the skip link is the 25th Tab stop | activatePage(name, opts) skips the focus move when opts.focus===false; the DOMContentLoaded call passes it, hashchange navigations still move focus. Verified: fresh deep-link load of #technique leaves focus on <body> (skip link is the first Tab stop), subsequent hash navigation focuses page-sets/page-season. | fixed |
| R1-A11Y-09 | visualizer | medium | Visualizer phase steps are mouse-only, 8.3 px text, low-contrast | Phase steps render as <button type=button class=phase-step aria-pressed>; updatePhaseBar3D keeps aria-pressed in sync; the bar is role=group 'Stroke phases'. .phase-step is now 0.62rem (9.92px, was 8.3px) in --white3 with button chrome reset; mobile breakpoints raised from 0.44/0.55rem to 0.58/0.62rem. Verified at 1280px (six 149x13 steps in the 40px bar, no overflow) and 375px; buttons focusable, aria-pressed follows the active phase. Merged with R1-VISUAL-04. | fixed |
| R1-A11Y-10 | ai, visualizer | medium | Same-state toggle groups lack aria-pressed (AI modes, viz speed) | aria-pressed added to the six AI mode buttons (not #mode-clear) and the three speed buttons, maintained in setMode() and setSpeed3D(). Verified initial states true/false and toggling. | fixed |
| R1-A11Y-11 | sets (6 tables, 18 th), timers (3 tables, 15 th), race (1… | medium | Data tables have <th> without scope and no captions | scope="col" added to every header-row <th> in the 15 thead rows (static tables and the calculator's JS templates); enhanceA11y also sets scope on any thead th and names each caption-less table (aria-label) after its tab (sets tables → 'AeT Work', 'Threshold'…) or the nearest preceding heading; the two timers result tables that have no preceding heading got explicit aria-labels ('Send-off schedule', 'Recorded splits'). Verified: 0 th without scope (was 43), 17/17 tables named. | fixed |
| R1-A11Y-12 | season (5 links), auth gate (Terms of Use, Privacy Policy) | medium | Links that open a new tab give no warning (season, auth gate) | New helper markNewTabLinks() appends an sr-only ' (opens in a new tab)' plus an aria-hidden ↗ glyph (skipped when the label already ends in an arrow) to every a[target=_blank]; run from enhanceA11y and again for the season view after renderSeasonPage so the dynamic meet links are covered. Verified: 0 unwarned new-tab links, season's 5 links (incl. 'TYR Futures Championships 2027') announce the new tab. | fixed |
| R1-A11Y-13 | technique (8 tabs, 909 px of tabs in 335 px), injury (8),… | medium | Tab strips scroll horizontally with the scrollbar hidden — no affordance that more tabs exist | .tabs gets a CSS mask driven by --tabs-fl/--tabs-fr; enhanceA11y measures each strip (scroll listener + ResizeObserver, so hidden views re-measure when shown) and sets a 40px right fade while more tabs are off-screen and a 28px left fade once scrolled. Verified at 375px on technique (scrollWidth 881 vs 335): right fade on at start, off at the end with the left fade on, both mid-way; no fade when a strip fits. Merged with R1-VISUAL-02. | fixed |
| R1-A11Y-14 | home (cards), all (nav brand), technique etc. (accordion … | low | Custom role=button elements use the browser default focus ring instead of the site ring | Shared focus-visible rule extended with [role="button"]:focus-visible and [tabindex]:not(.page):focus-visible, so the nav brand, metric cards and any remaining custom controls get the 2px cyan ring (the .page wrapper is excluded so programmatic route focus never outlines the whole view). Cards and accordions are now real links/buttons and inherit it anyway. Merged with R1-VISUAL-03. | fixed |
| R1-A11Y-15 | home, season (card tags), visualizer (`#motion-btn`) | low | Purple .card-tag and "AI Reconstruct" text fail 4.5:1 | Added --purple-text:#c084fc (≈6.4:1 on the card background) and used it for the three purple card tags and the #motion-btn 'AI Reconstruct' label; --purple stays for borders/glows. Verified computed colour rgb(192,132,252). Merged with R1-VISUAL-10 (final --purple-text #bf8cff). | fixed |
| R1-A11Y-16 | timers (`.tlt-seg button small`), all forms (`::placehold… | low | --white4 text/placeholders sit at 4.46:1 on --navy3 | --white4 lifted from #6d879f to #7590a8 (≈5.0:1 on --navy3, 4.46:1 before); it is only used for tertiary text, placeholders and timeline dots. Verified computed token. Merged with R1-VISUAL-10 (final --white4 #7b95ad, the higher-contrast value). | fixed |
| R1-A11Y-17 | technique, videos, sets, visualizer, ai, nav | low | Decorative emoji in tab and button labels are read aloud | Leading emoji on the 13 tab buttons, the 7 AI mode buttons, the 5 visualizer stroke buttons and the AI Reconstruct button are wrapped in <span aria-hidden="true">; the trailing ✦ on 'AI Coach', 'Talk to AI Coach' and 'Ask AI Coach' likewise. Verified: 0 tab/mode names start with a glyph, nav button name is 'AI Coach'. | fixed |
| R1-A11Y-18 | home | low | Hero heading reads "TRAINELITE.SWIM FAST." | Whitespace between the three block spans; accessible name/copied text is now 'TRAIN ELITE. SWIM FAST.' and the spans still stack (display:block, three distinct line tops verified). | fixed |
| R1-A11Y-19 | log | low | Training log gives no announced feedback on save / delete | Added <p id="log-status" class="sr-only" role="status" aria-live="polite"> after the entry list and a setLogStatus() helper; saveLogEntry announces 'Session saved — N logged.', deleteLogEntry 'Session deleted.', clearLog 'All sessions cleared.'. Verified live text after save and delete. Merged with R1-CODE-23: the visible role=alert notice announces the save; the sr-only status keeps announcing deletes and clears. | fixed |
| R1-A11Y-20 | all | low | <footer> lives inside <main> on every view; no contentinfo landmark | The home site footer is now <footer role="contentinfo" aria-label="Site footer"> so it is exposed as a landmark even though it sits inside the routed <main> (moving it out of main would show the full footer on every view and change the per-view one-liners, so the explicit role is the surgical option the finding offered). Verified 1 contentinfo landmark. | fixed |
| R1-A11Y-21 | all (dialog) | low | Auth-gate close button is title-only "✕" | Gate close button is <button type="button" aria-label="Close sign-in dialog"> with the ✕ aria-hidden and an explicit 44x44 box (verified 44x44 when the gate is open, same visual position). Merged with R1-VISUAL-07. | fixed |
| R1-A11Y-22 | 404.html | low | 404 page auto-redirects after 2.5 s with no way to stop it; its grey text is 4.3:1 | 404.html: redirect delay raised from 2.5s to 20s with a visible 'Stay on this page' button that clears the timer and updates the (role=status) note; .small colour changed from #607a92 to #8ba5bd to match index.html's lifted --white3. Verified live: clicking Stay cancels the redirect. Merged with R1-CODE-01 / R1-FUNCTION-18. | fixed |
| R1-A11Y-23 | privacy.html, terms.html, disclaimer.html, 404.html | low | Legal pages have no skip link or nav landmark; 404 has no <main> | privacy/terms/disclaimer wrap the back link in <nav aria-label="Back">; 404.html's wrapper is now <main class="wrap">. Verified by fetching each page. | fixed |
| R1-A11Y-24 | timers | low | Timers 3-2-1 count-in checkbox is 20×20 px | #page-timers .tlt-check input is 24x24 (was 20x20); verified computed size. | fixed |
| R1-A11Y-25 | planner | low | Planner "No PR — add one" links are 14 px tall | Planner 'No PR — add one' links get class pl-nopr with display:inline-block;padding:5px 0 (24px minimum target) in #page-planner CSS. | fixed |

### Code quality, performance & SEO (code) — 25 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R1-CODE-01 | 404 | high | 404.html redirects to itself on nested paths (infinite loop) | 404.html computes the site root once (project folder on *.github.io, otherwise '/') and uses it for the button, the favicon and the 2.5 s redirect, skipping the redirect when already at root; theme-color/color-scheme added. Reproduced at /deep/path/ (button and redirect pointed at the 404 itself, tab re-navigated forever); after: button -> root, redirect lands on the app. Merged with R1-FUNCTION-18 and R1-A11Y-22 (20 s cancellable redirect instead of 2.5 s). | fixed |
| R1-CODE-02 | all | medium | initReveal() leaks one IntersectionObserver per route change | initReveal() now reuses one module-level IntersectionObserver (_revealObs). Reproduced: instrumented constructor; after the fix 45 activatePage() calls across all 22 routes construct 0 observers (was one per call) with the 3 pending .reveal elements still observed. | fixed |
| R1-CODE-03 | workouts, visualizer | medium | Server-returned error text is written to innerHTML unescaped (AI workout + AI viz) | Both innerHTML error sites (workout generator, AI visualizer) wrap err.error in escapeHTML(). | fixed |
| R1-CODE-04 | all | medium | Google Fonts stylesheet is render-blocking | Google Fonts CSS is <link rel=preload as=style> promoted to a stylesheet on load, with a <noscript> stylesheet fallback; renderBlockingStatus is now 'non-blocking' and Bebas Neue / DM Sans still load. All declared variants (300, 500, italic 300) are used, so none were dropped. Note: the preload link is promoted to rel=stylesheet on load, so a post-load DOM check sees rel="stylesheet". | fixed |
| R1-CODE-05 | every page except home and zones | medium | 20 of 22 views have no heading element; page titles are <div class="section-title"> | All 23 <div class=section-title> became headings: the page-leading one on 20 views is <h1>, the secondary ones (home, visualizer 'Ask the visualizer', season 'Futures/Juniors') are <h2>; .section-title gained font-weight:400 and margin-top:0 so Bebas Neue is not synthetically bolded. Every page now reports exactly one h1 (home keeps its hero h1). Merged with R1-A11Y-01. | fixed |
| R1-CODE-06 | all | medium | supabase-js is loaded from a floating major tag with no pin or SRI | supabase-js pinned to @2.116.0/dist/umd/supabase.js (the packaged file, not jsDelivr's dynamic .min.js which its own banner says must not carry SRI) with integrity sha384 computed locally and cross-checked against jsDelivr's published sha256 for that file, crossorigin=anonymous. Page loads with supabase.createClient available. | fixed |
| R1-CODE-07 | PWA | low | sw.js stores the app shell up to three times and precaches files the app never requests | sw.js PRECACHE drops './', './404.html' and './og.png'; networkFirst stores every shell navigation ('/', '/?x', '/index.html') under the single INDEX key via shellKey() and looks it up the same way offline (privacy.html etc. keep their own keys). Verified with a Node harness. Merged with R1-FUNCTION-22 (shellKey also keys non-shell pages by origin+pathname). | fixed |
| R1-CODE-08 | visualizer / PWA | low | Versioned viz-human.js?v= is cached beside the precached un-versioned copy | staleWhileRevalidate keys cache-busted requests (?v=) by origin+pathname (assetKey) for both match and put, so ./viz-human.js?v=3.1.1 shares the precached entry; the loader's hard-coded '3.1.1' fallback is gone (window.TL_VERSION \|\| ''). | fixed |
| R1-CODE-09 | updates / PWA | low | sw.js reports an arbitrary "previous" version | sw.js picks the semantically newest stale cache via a cmpVer copy (harness: 3.0.1/3.1.0/2.9.9 -> 3.1.0) instead of the last key caches.keys() happened to return. Merged with R1-FUNCTION-22 (one cmpVer; the duplicate compareVersions was dropped). | fixed |
| R1-CODE-10 | all (print) | low | Print stylesheet hides elements that do not exist; the real toast/drawer print | Print block now hides #tl-upd-toast/.tl-upd-toast, #tl-palette, #nav-drawer, .nav-scrim and .tl-pal-navbtn (the real toast, palette and drawer); the unreferenced .nav-scroll, .grid-4 and .tech-img-* rules are deleted. Verified in CSSOM. | fixed |
| R1-CODE-11 | n/a | low | Comments cite files that are not in the repo | All 15 comments citing script.js, NOTES.md, VIDEO_PLAN.md, apply_facades.py and check_index.py are reworded to the inline modules; README.md gains a 'Maintainer notes' section that houses the season-calendar, predictor-exponent, pacing-pattern, intensity-reference and duplicate-id notes the comments now point to. Merged with R1-COPY-16/17. | fixed |
| R1-CODE-12 | n/a | low | Duplicate helper implementations across modules; no global lsGet | Global lsGet() added beside lsSet/lsRemove; the planner and What's New lsGet, analytics anLsGet and the three esc/anEsc copies now delegate to the app helpers; openPalette() closes the nav drawer first so the drawer's and the palette's body-scroll locks never stack (verified at 375 px: drawer closed, body lock cleared, focus back on the hamburger after closing the palette). The two setText/fmtDate pairs were left alone because they intentionally differ in behaviour. The dupcheck heuristic now counts three column-0 "lsGet" declarations; two are IIFE-local shims (brace depth 1) that delegate to the new global, verified in the browser. | fixed |
| R1-CODE-13 | videos/technique | low | Dead DEMO_CLIPS feature and its 40 lines of loader code | The dead DEMO_CLIPS map, initDemoClips, setClip and their DOMContentLoaded hook (42 lines) are removed; nothing else referenced them. | fixed |
| R1-CODE-14 | visualizer | low | console.info fires on every swimmer build | The swimmer-build console.info is gated on window.TL_DEBUG \|\| lsGet('tl_debug')==='1'. Verified: a full build ran (58k triangles) with no 'swimmer built' console entry. | fixed |
| R1-CODE-15 | all | low | Native form controls and scrollbars are not told the page is dark | <meta name=color-scheme content=dark> plus color-scheme:dark on :root (also on the 404 and legal pages); computed colorScheme is 'dark' so date pickers, selects, scrollbars and autofill render dark. | fixed |
| R1-CODE-16 | privacy, terms, disclaimer | low | Legal pages have no theme-color / manifest / apple-touch-icon | privacy/terms/disclaimer heads gain theme-color, color-scheme, SVG+PNG favicons, apple-touch-icon and the manifest link; verified in the browser. | fixed |
| R1-CODE-17 | SEO | low | sitemap.xml: stale lastmod and a comment naming a page that does not exist | sitemap comment names #timers; lastmod set from git (home 2026-09-17, privacy 2026-09-13, terms/disclaimer 2026-08-25) with a comment on how to refresh it; README release checklist gains step 4 (update lastmod). Merged with R1-COPY-15. | fixed |
| R1-CODE-18 | home | low | No JS-off fallback: the page is blank without JavaScript | <noscript> after the skip link shows #page-home, un-hides .reveal content and pins a short 'JavaScript is off' note to the bottom of the viewport. | fixed |
| R1-CODE-19 | home, videos, ai | low | Missing preconnects for origins used on first interaction | preconnect (crossorigin) for the Supabase project host and dns-prefetch for i.ytimg.com and www.youtube-nocookie.com. | fixed |
| R1-CODE-20 | PWA | low | Manifest lacks id, lang, orientation, categories and screenshots | site.webmanifest gains id './', lang, dir, orientation 'any', categories [sports, health, education] and a wide screenshot (og.png 1200x630); still valid JSON and fetches fine. | fixed |
| R1-CODE-21 | home / SEO | low | Structured data and social meta are minimal | JSON-LD is an @graph of Organization (logo), WebSite (publisher) and WebApplication (SportsApplication, Web, free offer, featureList, image); og:locale and og:image:type added. No twitter:site was invented. | fixed |
| R1-CODE-22 | all hash pages / SEO | low | Hash routes share one description/canonical and cannot be indexed individually | Not implemented: a 0-second meta refresh is treated by search engines as a redirect, so the proposed thin landing files would be consolidated into '/' rather than indexed; a real fix needs per-route pages with their own content and an owner decision on public URL structure (new URLs, sitemap, SW scope) - a feature, not a surgical fix. The sitemap comment error from R1-CODE-17 is corrected. | deferred |
| R1-CODE-23 | calculator, log, profile, workouts, ai … | low | Fifteen blocking alert() dialogs used for validation | tlNotice(id,msg,kind) helper writes into a <p class=form-notice role=alert> under each form (golf, splits, pace zones, converter, training log, AI visualizer, My PRs backup) and focuses the offending field; 14 of the 15 alert() calls are replaced, with success notices for 'Session saved.' and backup import. The one remaining alert is the sign-in-service-unavailable case (no form; exceptional) and tlNotice falls back to it only when a form has no notice element. Verified: no alert fired, notice text/role/focus correct. Merged with R1-FUNCTION-07/09/12 and R1-FUNCTION-15: the function lens's richer validation messages are kept, delivered through tlNotice. | fixed |
| R1-CODE-24 | visualizer, log | low | Repeated 150-character inline style strings and inline mouseover handlers in rendered HTML | .viz-fallback replaces the two identical 150-char inline style strings; the log delete button is class log-del with CSS :hover/:focus-visible colouring, an aria-label and title, and no inline handlers. Merged with R1-FUNCTION-02 / R1-A11Y-06. | fixed |
| R1-CODE-25 | all | low | 3,703 DOM nodes in the initial document | Not implemented: the timers, planner and analytics modules all initialise against their markup at DOMContentLoaded, enhanceA11y decorates every page at load, and the command palette builds its search index from every page's headings - stamping those views from <template> needs a coordinated lazy-init refactor across four modules and would leave palette search blind to un-stamped pages. That is beyond a surgical edit; flagged for a dedicated pass. | deferred |

### Swimming domain accuracy (domain) — 31 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R1-DOMAIN-01 | visualizer | high | The breaststroke technique pass renamed/re-timed the five phases in STROKE_PATCHES but never re-mapped drag/thrust/lift/vel/eff, which stay on the old slots in STROKES.breaststroke (index.html). | viz-human.js: each breaststroke patch phase now carries drag/thrust/lift/vel/eff (Glide 16/4/2/1.80/83, Out-Sweep 44/70/18/1.64/63, In-Sweep+Heel-Draw 88/96/24/1.35/48, Squeeze 48/140/24/1.85/72, Streamline Glide 20/6/2/1.72/80) and applyPatches copies the five physics fields (new PHYS list) alongside dur. Verified in the browser: STROKES.breaststroke.phases after patching show the re-keyed values. | fixed |
| R1-DOMAIN-02 | calculator | high | Zone percentages of the best 100 are a full zone too fast. | calcPaceZones pct -> Z1 [1.35,1.50], Z2 [1.22,1.35], Z3 [1.12,1.20], Z4 [1.04,1.10], Z5 [0.90,1.02]; a note under the table points to the Critical Swim Speed test in Analytics for a measured threshold. 1:00 input now gives threshold 1:07.2-1:12.0, AeT 1:13.2-1:21.0. | fixed |
| R1-DOMAIN-03 | calculator | high | the negative/positive-split ramp is a fixed 0.4 s per 50 step that scales with rep count. | calcSplits now calls the planner's buildSplits (exported via window.tlPlanner, START_BONUS added to the export) with a 2% half-difference and the LC dive bonus (1.4 s) off the first 50; 'surge' keeps a local percentage pattern renormalised to the target. 1500 @ 16:00 negative -> 31.27 / 32.63 ... 31.42 (sum 16:00.00); 200 @ 2:00 even -> 28.95 / 30.35 x3. Merged with R1-FUNCTION-03/04. | fixed |
| R1-DOMAIN-04 | technique | high | Elite underwater dolphin-kick frequency is 1.9 ± 0.3 Hz (≈100–130 kicks/min), up to ≈2.5 Hz (150/min) in the very best sprinters — the site's own viz-human.js sources say 1.46 / 1.75 / 2.11 Hz at 70 / 80 / 90 % speed and What's New calls 96 kicks/min "realistic". | Technique > Underwater 'Elite rate' -> '~2.0-2.5 Hz (120-150 kicks/min) - frequency, not amplitude, tracks speed.' Merged with R1-COPY-02 (see that row for the final wording). | fixed |
| R1-DOMAIN-05 | race | medium | "100m target: front half no more than 2 % faster than back half. | Race > Pacing Science rewritten (100: first 50 normally 2-3 s faster, minimise the fade; 200: second 100 normally 2-3 s slower, hold the third 50 at the second's pace; 400+: even 100s with a lift at the end); event table '200 Free - Even effort, hold the third 50'. Merged with R1-COPY-01. | fixed |
| R1-DOMAIN-06 | technique | medium | 200 IM leg shares. | Technique > IM elite split -> 'fly ~22%, back ~25%, breast ~28-29%, free ~25%' (matches planner PATTERNS). Merged with R1-COPY-04. | fixed |
| R1-DOMAIN-07 | zones / taper / workouts | medium | "HR 24–28 Golf scale", "HR 27–31 Golf scale", "HR below 22 Golf scale", and the workout-generator prompt "HR targets on Golf scale". | 'Golf scale' removed from the Zone 2/3 cards ('(10-second count)'), taper Active Recovery ('on a 10-second count') and the workout-generator prompt ('HR targets as a 10-second pulse count (e.g. 24-28 = AeT)'). grep 'Golf scale' -> 0. Merged with R1-COPY-05. | fixed |
| R1-DOMAIN-08 | visualizer | medium | Net force is clamped at 0, so every deceleration phase (breaststroke glide −12 N, backstroke entry −12 / recovery −14, butterfly exit / recovery / entry −14 / −18 / −10, dolphin load −14) shows "0 N". | Net-force readout is signed (+/-), negatives coloured red; legend reads 'Net Force (+ accelerating / - decelerating)'. Verified: dolphin Heels Up shows -24 in red, freestyle Pull +76. | fixed |
| R1-DOMAIN-09 | visualizer | medium | Freestyle thrust exceeds drag in all six phases (net +34, +24, +76, +46, +37, +90 N; | Freestyle thrust: Entry 60->20, Catch 64->36, Recovery 70->28, Reach Forward 120->24; net per phase now [-6,-4,+76,+46,-5,-6]. | fixed |
| R1-DOMAIN-10 | visualizer | medium | Dolphin-kick velocity 2.40–2.62 m/s across the whole cycle is faster than the 50-free world-record average (≈2.4 m/s incl. | Dolphin velocities 2.05/1.95/2.25/2.15/2.10 in the index.html record; the viz-human.js dolphin patch re-keys physics per phase (Heels Up 34/10/4/2.05/70, Downbeat Whip 40/120/40/1.95/78, Downbeat Peak 46/154/60/2.25/84, Upbeat Drive 33/84/38/2.15/79, Upbeat Peak 24/50/26/2.10/85). | fixed |
| R1-DOMAIN-11 | visualizer | medium | Butterfly was re-authored to 7 phases but the physics stayed on the old 6 slots: "Push — the fastest part of the pull" reads vel 1.96 / thrust 100 while "Keyhole In-sweep" reads the old Keyhole-Pull peak (vel 2.14, thrust 158). | Butterfly patch phases carry their own physics (Press 38/44/1.72, Catch 54/84/1.88, Keyhole 62/120/2.00, Push 70/158/2.14, Exit 50/90/1.98, Recovery 40/22/1.66, Entry unchanged); Push now shows the peak 158 N / 2.14 m/s. | fixed |
| R1-DOMAIN-12 | visualizer | medium | the jump sets clockT exactly on the phase boundary; | jumpToPhase3D sets clockT = t + 1e-4 so the driver lands inside the tapped phase. Verified: breaststroke jump 3 -> idx 3 'Squeeze into the Glide' with 48 N / 140 N; butterfly 3 -> 'Push'; dolphin 1 -> 'Downbeat Whip'. | fixed |
| R1-DOMAIN-13 | zones | medium | Zone 4 "HR 31–35" on the 10-second count = 186–210 bpm; | Zone 4 HR 31-35 -> 30-33 in the zones bar row and calcPaceZones desc; the Zone 4-5 card (which had no HR) now states 'HR 30-33 (10-second count)'. Post-merge fix also aligned the calculator's Zone 1 desc to "HR <22". | fixed |
| R1-DOMAIN-14 | season | medium | Futures has ONE national time standard; | Season > Futures: 'One national time standard; you swim at the site for your zone. Sectional cuts, by contrast, vary by zone and LSC.' | fixed |
| R1-DOMAIN-15 | workouts | medium | the prompt orders 3,500–5,000 m per hour regardless of level (5,000 m in 60 min is 1:12/100 including rest), so age-group and masters sessions come back at elite density. | Workout prompt volume targets scale with the Athlete Level select (age-group/masters 2000-2800 ... 4000-5500; senior 2500-3300 ... 5000-6500; elite unchanged) via a local VOL_TARGETS map. | fixed |
| R1-DOMAIN-16 | technique | medium | 1.2–1.5 body lengths ≈ 2.2–2.7 m — far too early; | Flip turn: 'Initiate the flip about one body length out - roughly 1-1.5 m from head to wall, as the T passes under you.' | fixed |
| R1-DOMAIN-17 | technique | medium | a pinky-first backstroke entry has the palm facing OUT with the thumb UP; | Backstroke entry: 'hand rotated so the palm faces out - pinky down, thumb up.' | fixed |
| R1-DOMAIN-18 | timers / analytics | medium | three stroke-rate tables on the site disagree while citing the same sources: timers 50 FR 56–64 vs analytics men 58–67; | Timers stroke-rate table merged outward (data-lo/hi, cycles, s/cycle and strokes/min columns): 50 FR 56-67, 100 FR 46-55, 200 FR 38-50, 400 FR 38-53, 800/1500 35-55 (sub-note ~35-40 / ~45-55), 100 BR 44-58, 200 BR 31-43. Merged with R1-COPY-03: the Timers table now renders from the shared ELITE_RATES constant with these bands. | fixed |
| R1-DOMAIN-19 | visualizer | low | freestyle "Entry & Extension" text says roll ~45° while the rig rolls ±22° (rollAmp: -22 in the patch; | Freestyle entry text: 'Body roll ~25-45 degrees depending on distance (the model shows a distance-pace roll)'; rig untouched. | fixed |
| R1-DOMAIN-20 | nutrition | low | swimmers sweat markedly less than land athletes (~0.3–0.6 L/h vs 1–2 L/h); | Hydration: 'you still sweat in the pool, typically 0.3-0.6 L per hour, and never notice it.' | fixed |
| R1-DOMAIN-21 | mental | low | power posing failed replication (Ranehill et al. | Mental: 'power posing 2 min' -> 'explosive activation (arm swings, 3-5 jump squats)'; self-talk cue 'This is what you train for.' | fixed |
| R1-DOMAIN-22 | sets | low | 8×100 → 6×100 → 4×100 → 2×100 is a descending ladder (2,000 m), not a pyramid. | Sets: 'Kick Pyramid' -> 'Kick Ladder (2,000 m)'. | fixed |
| R1-DOMAIN-23 | sets | low | warm-up raises MUSCLE temperature 1–2 °C; | Warm-up: 'Raises muscle temperature 1-2 C.' | fixed |
| R1-DOMAIN-24 | dryland | low | "3×10 GIRD + sleeper stretch" — GIRD is a diagnosis (glenohumeral internal-rotation deficit) and the sleeper stretch is a timed hold, not 10 reps. | Dryland: '3x30s Sleeper stretch (for GIRD)'. | fixed |
| R1-DOMAIN-25 | analytics | low | any value ≥ 1.8 m is reported "inside the range … roughly 1.8–2.7 m per cycle", including 2.78 m (verified with 50 m / 31.4 s / 36 strokes). | calcStrokeValues: new dpcM > 2.7 branch ('above the elite competition range ... a push-off and glide inside the measured segment inflates distance per cycle'). Verified: 50 m / 31.4 s / 36 strokes -> 2.78 m 'above'. | fixed |
| R1-DOMAIN-26 | technique | low | header promises the grab start; | Starts: added the grab-start sentence (both feet at the front edge, hands gripping the block; relay take-overs) under the GRAB START & TRACK START header. Merged with R1-COPY-31. | fixed |
| R1-DOMAIN-27 | home | low | "6 New Sections — Race, Nutrition, Mental…" is stale (those pages predate 2.0; | Home metric '6 New Sections' -> '3 Deck Tools - Timers, Planner, Analytics'. Merged with R1-COPY-08. | fixed |
| R1-DOMAIN-28 | visualizer | low | (mannequin-fallback text) the upkick is driven by hip extensors / hamstrings; | Mannequin-fallback Upbeat Drive: 'The hips rise as the glutes and hamstrings fire'. | fixed |
| R1-DOMAIN-29 | technique | low | measured kick contribution is ~10–15 % at distance pace and up to ~30 % in sprint; | 6-beat kick: 'The kick supplies up to ~30% of propulsion in sprint freestyle (~10-15% at distance pace).' | fixed |
| R1-DOMAIN-30 | sets | low | "rest per 25 m of effort" reads as a rate; | Speed work: 'Full recovery mandatory - 2-5 min between efforts, longer for longer reps.' | fixed |
| R1-DOMAIN-31 | injury | low | breaststroker's knee is a medial (MCL) valgus + external-rotation overload; | Breaststroker's knee: 'Primary cause: valgus/rotation load from a wide-knee recovery and kick volume; hip abductor/adductor weakness adds to it'. | fixed |

**Round 1 totals:** 163 findings — 161 fixed, 0 partial, 2 deferred, 0 not reproduced. Post-merge fix commit: "forensic r1: post-merge fix" (calculator Zone 1 HR ceiling).

## Round 2 — 2026-09-18

Round focus: second full sweep of 3.1.1 after the round-1 merge (a836a04), with each lens first re-checking every round-1 fix for regressions and then going deeper — every tab, accordion, dialog and dynamic render on all 22 hash routes at desktop, 768, 375 and 320 px; the calculators, log, backup, planner, timers and analytics driven with concrete inputs; the 3D lab measured per phase on the human rig; the service worker, CSP, SRI, fonts, sitemap, robots and the legal pages; the swimming facts re-verified against the primary sources. Fixes were made on six branches (forensic/r2-copy, -visual, -function, -a11y, -code, -domain) and merged into main one by one; 44 conflict hunks (visual 2, function 3, a11y 21, code 9 + viz-human.js 1, domain 10) were resolved by hand keeping every lens's intent — the cross-references are in the Fix column. Post-merge: check_index.py PASS, dupcheck against HEAD~6 (no new duplicate top-level names; 19 new names, 2 removed helpers with no remaining references), node --check viz-human.js and sw.js, version strings untouched (sw.js VERSION = UPDATES[0].version = footer = 3.1.1). Browser smoke of the merged site (own tab, served copy on :8655): all 22 pages open with no console errors beyond the pane's known service-worker registration failure and no CSP violations; every route title carries " — Tidelyne"; the 3D lab builds under the new SRI/CSP and all five strokes switch and render (skinned human rig in the scene), play/pause, model, X-ray, forces, view tweens and arrow-key phase stepping work; the drawer (52 px head, close button on the burger's box) and the ⌘K / "/" palette open, filter, route and close; calculators run through calcFail with aria-invalid/aria-describedby, sr-only captions and the 19 s split guard; Enter submits calculator, generator and log panels once; the log sorts by date, rejects future dates, hides the stale 30-day summary and re-fills today's date; PR fields debounce; backup export/import asks before overwriting and round-trips; the tempo trainer's spm↔cpm round trip restores 65; the auth dialog traps focus on the close button and re-opens cleanly after a fast close; the What's New card shows the merged service-worker message; privacy/terms/disclaimer/404 use the promoted font preload; sitemap.xml parses. Two post-merge fixes were committed ("forensic r2: post-merge fix"): the duplicated Enter-to-submit handler collapsed into one, and the last dead NOTES reference in viz-human.js trimmed.

### Content & copy (copy) — 25 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R2-COPY-01 | analytics (Stroke rate & DPS tab) | high | Analytics › Stroke rate & DPS: the form panel is 731 px wide on a 375 px phone — the whole page scrolls sideways | Reproduced (Analytics › Stroke rate & DPS: scrollWidth 751 at 375 px, .an-gen-row tracks 338.8/338.8/0, panel 730.8 px). CSS: .calc-wrapper collapses to minmax(0,1fr) at ≤700 px, .calc-panel min-width:0, .gen-row uses minmax(min(190px,100%),1fr) with .gen-row>*{min-width:0}. After: 375 px, one 297.8 px column, every label/select/result inside the viewport; Workout Generator row and all calculator tabs still 375; desktop tracks unchanged (3×203 px on Workouts). Merged with R2-VISUAL-01/02, R2-A11Y-02 and R2-COPY-08: one `.calc-panel{min-width:0}`, `.calc-wrapper` collapsing to `minmax(0,1fr)` ≤700 px, `.gen-row` `minmax(min(190px,100%),1fr)` with `.gen-row>*{min-width:0}`, and `.form-group`/`.form-select`/`.form-input` `min-width:0` shipped together. | fixed |
| R2-COPY-02 | calculator (Time Converter) | medium | Time Converter prints "-12.3% faster" — a minus sign and "faster" at once | Reproduced ('-12.3% faster'). diffStr now shows the magnitude with the word only: '12.3% faster', '2.6% faster', '14.0% slower', 'Your input' (dropped the '+' too so both directions read the same way). Verified live with 1:52.44 from LCM. | fixed |
| R2-COPY-03 | race (Meet Day tab) vs mental (Focus & Cues, Pre-Race Routine) vs planner (Meet Countdown) | medium | How many focus cues? Race Day says one, Mental and the Planner say two | Race › Behind the Blocks bullet is now 'One or two focus cues only — more than that is too many (see Mental › Focus & Cues)' with a working showPageTab('mental','m-focus') link (lands on m-focus, verified); Mental and Planner keep 'two at most'. Wrapped the bullet in a <span> because .content-block li is a flex row and an inline link mid-sentence split into three flex items. | fixed |
| R2-COPY-04 | s: race, updates, timers, planner, disclaimer.html, 404.html. Files/anchors: index.html `so the job is to minimise the fade` (line 2156 — written by the R1-COPY-01/DOMAIN-05 rewrite), `keyboard-operable and labelled for screen readers` (UPDATES 2.1.0, ~9626), `black minute hand (drawn grey here` (2628) and the SVG `<title>` "…black minute hand (drawn grey here)" (2608), CHECKPOINTS `Early night. Lay out kit.` (7428); disclaimer.html `personalised medical or dietetic advice` (line 61); 404.html `'Redirect cancelled — use the button above` (line 51) | medium | Regression of R1-COPY-10: British spellings remain (one introduced by a round-1 fix) | minimize (race pacing), labeled (UPDATES 2.1.0 text — a one-word spelling fix inside an existing entry, no version/entry structure touched), gray ×2 (pace-clock SVG title + info box), 'Lay out your gear.' (checkpoint d:1), personalized (disclaimer.html), canceled ×2 (404.html status text + its comment). No release spelling grep exists in the repo to extend; code-only comments left alone as in R1-COPY-10. | fixed |
| R2-COPY-05 | timers | medium | Timers sound note is now wrong for the interval timer | Confirmed the visibilitychange handler pauses only TP. Timers note now: 'The tempo trainer pauses itself if the screen is hidden (press Resume). The interval timer keeps counting in the background, but many browsers mute its beeps until you return, so keep the screen on for sound.' | fixed |
| R2-COPY-06 | planner (Meet Countdown) | medium | Checkpoint "Race-day nutrition →" lands on the Daily Fueling tab, not Race Day | Reproduced (click landed on n-daily). CHECKPOINTS gained an optional pageTab field ('n-race' on day 3), rendered as data-page-tab; the existing delegated click handler on #pl-mt-checkpoints now calls showPageTab(page, tab) for such links (no inline handlers). Verified: click → page-nutrition, active tab n-race / 'Race Day'. | fixed |
| R2-COPY-07 | every hash page except home and updates | medium | 20 of 22 page titles omit the product name | setPageTitle appends ' — Tidelyne' unless the title already contains the product name (home, updates), so PAGE_TITLES itself is untouched and the palette's pageLabel() still derives short labels from it. Verified: 'AI Swim Coach — Tidelyne', 'Training Log & Weekly Volume — Tidelyne', home and What's New unchanged (no doubling). Merged with R2-CODE-16: the copy lens's rule (append " — Tidelyne" unless the title already names the product) is the one shipped, over the code lens's shortened PAGE_TITLES entries; every route title ≤ 60 characters. | fixed |
| R2-COPY-08 | s: planner, timers | low | Two more 375 px overflows: Planner › Race Predictor (391 px, new since round 1) and Timers › Tempo Trainer (396 px) | Reproduced (Race Predictor 391 px with two 370 px panels; Tempo Trainer 396 px with .tlt-vh at x=394.7). R2-COPY-01 CSS brings the predictor to 375 (panels 335 px); left:0 added to #page-timers .tlt-vh and to the shared .sr-only rule — timers now 375, span x=0. Merged with R2-VISUAL-03 / R2-A11Y-03: `.table-wrap{position:relative}`, `left:0` on `.sr-only` and `.tlt-vh`, and the timers header span on the shared `.sr-only` class. | fixed |
| R2-COPY-09 | planner vs nutrition | low | Carb-loading window: checkpoint says "two days before" for every meet, Nutrition says the day before | Day-3 checkpoint now reads 'Carbohydrate intake up to 8–10 g/kg on the day before racing (start two days out for a multi-day meet), hydrate steadily, no new foods.' — matches the Nutrition page it links to. Verified rendered in the countdown. | fixed |
| R2-COPY-10 | log | low | Log distance error message says "m" although the field now accepts metres or yards | Validation notice is 'Distance must be between 0 and 100,000 (m or yd).' (verified live still saying 'm' before the fix). | fixed |
| R2-COPY-11 | README.md, anchors `Three things carry the version` (line 77) and `Personal data (swimmer name, PRs, training log, Race Planner goals and meets)` (Data & privacy) | low | README: "Three things … must move together" lists four, and the data list omits what the v2 backup now holds | README: 'Four things carry the version number…'; Data & privacy now lists 'timer and analytics settings' and says the backup downloads all of it. | fixed |
| R2-COPY-12 | privacy.html, anchors `deliver fonts and the 3D engine.` (line 98) and `plus the messages you send to the AI coach.` (line 57) | low | Privacy §4 under-describes jsDelivr; the "short version" names only the AI coach | privacy.html §4: 'deliver fonts, the sign-in library and the 3D engine.'; short version: '…plus whatever you type into the AI features (coach messages, workout-generator settings and notes, visualizer prompts).' | fixed |
| R2-COPY-13 | s: analytics (Weekly numbers cards), timers (elite-rate table header) | low | Unit symbols are uppercased by CSS: "21,000M/YD", "S PER CYCLE" | Reproduced ('21,000M/YD', 'S PER CYCLE'). .an-period-big small is text-transform:none with a thin space (&thinsp;) before m/yd (innerText '4,000 m/yd'); chart tooltip total gets the same thin space; a shared .data-table th .th-unit{text-transform:none} span wraps units in the analytics weekly table ('Total (m/yd)'), the calculator Race Splits ('50 m Split') and Pace Zones ('Per 100 m') headers; the timers header is now 'Seconds / cycle' (matches 'Cycles / min', no case-sensitive symbol in an uppercase header). | fixed |
| R2-COPY-14 | s: log, analytics, zones, calculator, ai | low | Zone names differ between the Log, Analytics, Zones and Calculator: "Z4 VO2 Max" / "Z4 VO2 max" / "Z4 VO₂" / "Zone 4 — VO2 Max"; "Z5 Speed" vs "Z5 Sprint" | One label set — Z1 Recovery, Z2 AeT, Z3 Threshold, Z4 VO₂ max, Z5 Sprint — in the log select (values too; zoneKey() parses by Z-prefix so old 'Z4 VO2'/'Z5 Speed' entries still classify), ZONE_SERIES labels, the calculator zones table ('Zone 4 — VO₂ max'), the Zones bar ('Z4 VO₂ max', fits at 375: 219 px) and the AI quick prompt. PAGE_TITLES keeps plain 'VO2 Max' for the tab/SEO title. | fixed |
| R2-COPY-15 | s: technique, sets, race, taper (no space) vs timers, planner, analytics (space) | low | Number–unit spacing is inconsistent page to page ("1,200–1,400m" vs "50 m") | SI spacing applied site-wide: 50 replacements in the HTML text (technique, sets, race, taper, mental, calculator labels/options, season, home metric '15 m', set name 'UW 15 m IM Drill'), plus the user-facing calculator/season JS strings ('per 50 m', 'Enter a valid 100 m time', 'long-course (50 m)'), the Race Splits rows ('50 m', '100 m'…) and the log entry meta ('4,000 m · 75 min'). Data keys (option values '25m'/'50m', poolLabel map, meet name '(25m)') and AI-prompt strings left untouched. Zero unspaced number+unit left in the HTML section. Merged with the domain lens's rewrites (R2-DOMAIN-02/06/09/10/11/14): the domain wording shipped with the SI spacing applied ("45–55 cm", "1,150–1,350 m", "per 50 m", "100 m freestyle"). | fixed |
| R2-COPY-16 | s: visualizer (info panel), log | low | Hyphens instead of en dashes in the 3D-lab phase text and a log placeholder | '10–15°', '3–4×', '60–70%' in the STROKES phase descriptions; Main Set placeholder 'HR 24–28' (verified rendered). Merged with R2-DOMAIN-01: the domain lens's new physics numbers on the STROKES rows, the copy lens's en-dash/× descriptions. | fixed |
| R2-COPY-17 | technique (Underwater tab) | low | Technique › Underwater: "elite means sit near 1.9 Hz" reads as a verb phrase | Elite-rate key point now '…at full race speed; the elite average is closer to 1.9 Hz. Rate, not amplitude, is what rises with speed.' Superseded by R2-DOMAIN-10 in the merge: the key point now reads "About 1.9 Hz on average at race speed (≈115 kicks/min), with the fastest sprinters at 2.0–2.5 Hz (120–150)…". | fixed |
| R2-COPY-18 | sets (AeT Work, Threshold tabs) | low | Sets page: two missing articles/prepositions and "RDs"/"Rd"/"rounds" written three ways | 'and the majority of Meso II'; 'first appear in Meso I, Week 4'; '3 rounds: 3×400 descending'; 'Rd 1: sub-threshold'. Verified rendered. | fixed |
| R2-COPY-19 | season (Cuts & Motivational Standards) | low | Season: "Motivational Times" and "Motivational Standards" for the same document | 'Motivational Times' throughout: section eyebrow 'Cuts & Motivational Times', the warn-box, the card body and the link 'View official Motivational Times →'. | fixed |
| R2-COPY-20 | season | low | Season: the Futures host-site notice says "sites to be announced" three times | SEASON_TBA entry: name 'TYR Futures Championships 2027', expected 'host sites to be announced — dates are set (Jul 28–31, 2027)'; table tag 'Zoned · multiple sites'. Rendered notice now says it once. | fixed |
| R2-COPY-21 | s: ai, race, nav | low | The same feature is named differently in the nav, the page and the palette ("COACH AI", "Race Day") | Chat header 'AI SWIM COACH'; nav label 'Race Strategy' (the palette breadcrumb derives from the nav, so it follows); planner fine print and the race-day phase text now say 'Race Strategy' / 'Meet Day timeline on the Race Strategy page'. PAGE_ALIASES keeps 'race day' as a search alias. | fixed |
| R2-COPY-22 | home | low | Home AI card bullet "Full program knowledge" is leftover program jargon | Home AI card bullet: 'Knows every page of this site — sets, zones, taper, race plans'. | fixed |
| R2-COPY-23 | home | low | Home footer still omits the two flagship features (3D Visualizer, AI Coach) | Footer: '3D Visualizer' added to Learn (after Technique Library) and 'AI Coach' to Tools (first). Verified in the rendered footer. | fixed |
| R2-COPY-24 | viz-human.js, index.html | low | Comments still cite files that are not in the repo (viz-human.js) and a build-time insertion note (index.html) | Comment-only: viz-human.js no longer cites NOTES-motion.md, NOTES-body.md, preview/NOTES.md, strokes.json, 'NOTES §9/§11' or bare '(see NOTES)' (8 mentions; STROKE_PATCHES now points at each patch's own notes string, strokes.json → 'the STROKES table in index.html'); index.html palette comment drops '(bundle: palette; insert just before </main>)'. node --check passes. Merged with R2-CODE-11 (the code lens's README "Maintainer notes" pointer kept). One leftover "(see NOTES-motion.md)" inside the freestyle patch notes string was trimmed in the post-merge fix; grep NOTES finds nothing in viz-human.js now. | fixed |
| R2-COPY-25 | all (⌘K palette) | low | Palette quick actions duplicate two page entries and one is labelled with its own breadcrumb | The two page-only actions are real actions: 'Plan race splits' opens the planner's Split Planner tab and focuses #pl-sp-goal; "See this week's volume" opens Analytics and scroll-highlights #an-periods (both verified via the palette). Their search keys are kept/extended. | fixed |

### Visual & layout (visual) — 23 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R2-VISUAL-01 | analytics (tab "Stroke rate & DPS") | high | Analytics › "Stroke rate & DPS" reference table blows the two-column grid apart at every width | Reproduced (grid 248/752, scrollWidth 1044 at 748; wrapper table 692px). .calc-panel{min-width:0} so the grid item can shrink and .table-wrap contains the table; the reference table keeps values nowrap but lets its headers wrap and drops cell padding to 8px, so it measures 376px and fits the 376px desktop panel with no scroll. After: grid '1fr 1fr', panel right edge 1086 at 1280; scrollWidth 375 at 375 (table scrolls inside the 296px wrapper, with the new .table-wrap edge fade) and 763 at 768. See R2-COPY-01 for the merged CSS. | fixed |
| R2-VISUAL-02 | planner (tab "Race Predictor") | medium | Planner › "Race Predictor" panels overflow the page by 16 px (375) / 38 px (768) | Reproduced by reverting min-width in-page (panels 355px in a 335px column at 375). Fixed by the same .calc-panel{min-width:0} plus .form-select{min-width:0;max-width:100%}. After: both Race Predictor panels 335px at 375 and 350px at 768; documentElement.scrollWidth 375 / 763. See R2-COPY-01 for the merged CSS. | fixed |
| R2-VISUAL-03 | timers (all tabs; the offending markup is in the Tempo Trainer preset table) | medium | Timers page is 396 px wide on a 375 px phone because a visually-hidden span escapes the table wrapper | Reproduced (scrollWidth 396 at 375, .tlt-vh at x=395 with offsetParent BODY, .table-wrap static). .table-wrap{position:relative} so every absolutely positioned sr-only span (timers 'Load into the tempo trainer', season labels) is clipped by the wrapper's overflow. After: scrollWidth 375 on all three Timers tabs; no element outside a scroll container past x=375. See R2-COPY-08 for the merged CSS. | fixed |
| R2-VISUAL-04 | technique | medium | Technique tab strip wraps into a 7 + 1 row with "🏆 IM" orphaned under Freestyle (≥900 px) | Reproduced (8 tab tops {334,378} at 1280, 909px in 896; 6+2 at 900). #page-technique .tab-btn padding 11px 13px from 900px up (861px) and 11px 10px in the 900-999px band where the container is only 831px (813px). After: one row at 900 (813/831) and at 1280 (861/896). The generic >=900 wrap rule stays as the safety net. | fixed |
| R2-VISUAL-05 | videos (tabs Backstroke, Breaststroke, Butterfly, Dryland, Race Analysis; Underwater shows two 576 px cards) | medium | Video Library: five of seven tabs show one 1176 × 787 px video card | Reproduced (five tabs '1176px 0px 0px', card 1176x787). .video-grid uses repeat(auto-fill,...) so empty tracks stay, and a lone card spans the row but is capped: .video-grid>.video-card:only-child{grid-column:1/-1;max-width:640px}. After: lone cards 640x486 at 1280 (640x487 at 768), Freestyle and Underwater 376px cards on the same scale. | fixed |
| R2-VISUAL-06 | season | medium | Season meet table on phones: 127–170 px-tall rows, dates broken over four lines | Reproduced (row heights 170/127, Dates cell on 4 lines, Meet on 11). At <=480 the meet table renders one card per meet: thead hidden, tr a 2-column grid (name+tag full width; dates left with status right; course as a small mono line), with sr-only ::before labels ('Dates: ', 'Course: ', 'Status: ') for AT and role=table/rowgroup/row/columnheader/cell on the markup and the JS row template so the semantics survive display:block. After: rows 105-129px, every value on one line (screenshot checked); desktop table unchanged. Merged with R2-A11Y-19: the season table keeps the explicit table/rowgroup/row roles (display:block cells) and carries aria-labelledby="season-calendar-eyebrow". | fixed |
| R2-VISUAL-07 | log (with at least one saved session) | medium | Training-log entries squeeze all content into a 157 px column on phones | Reproduced with three seeded entries (.log-content 157px at x=127, cards 322/115/364). <=480: .log-entry wraps (gap 0.35rem 1rem, padding 1rem 1.1rem), .log-date flex 1 1 auto/order 0 centred, .log-del order 1, .log-content flex 1 1 100%/order 2. After: content 298px wide, date and the delete button share the first line, cards 238/116/280. | fixed |
| R2-VISUAL-08 | timers (Tempo Trainer) | medium | Timers preset table: the only interactive column ("Load") is off-screen on phones with no scroll cue | Reproduced (Load buttons at x=395-456, wrapper 333/445, no fade on .table-wrap). <=480 hides the derivable 's per cycle' column and lets 'same as cycles' wrap, so the preset table measures 333px and the Load buttons end at x=344 (screenshot checked); enhanceA11y's .tl-scroll-more fade now also covers .table-wrap, verified toggling on the analytics reference table (true at scrollLeft 0, false at the end). | fixed |
| R2-VISUAL-09 | visualizer (stroke / view / swimmer / speed rows), ai (mode chips) | medium | Hover state is identical to the selected state on the visualizer and AI-mode toggle groups | Confirmed the shared .active,:hover rules. Split them: .viz-btn.active / .ai-mode-btn.active keep the filled cyan look, and hover is a lighter cue only under @media(hover:hover) and only on :not(.active) (white text + cyan border, no fill), so touch devices get no sticky hover and the current selection is unambiguous. Rules verified in the live stylesheet. | fixed |
| R2-VISUAL-10 | updates (badges/tags), home (hero stats, metric labels, card tags), dryland, nutrition, planner, palette, technique (key-point labels), zones/season (badges), sets/season (ptag), videos (video-tag), every footer | medium | A family of 15 micro-labels renders at 8.8–9.3 px (0.55–0.58 rem) | Confirmed 8.8-9.3px computed sizes. Added --fs-micro:0.62rem to :root and used it in the 15 listed rules plus .footer-bottom p (.hstat-lbl, .card-tag, .badge, .kp-label, .ptag, .metric-card .lbl, .video-tag, .dl-day, .nutr-time, .pl-goal-label, .tl-pal-group, .tl-upd-stat-lbl, .tl-upd-badge, .tl-upd-tag, .tl-upd-counts). After: 9.92px; the 74px What's New tags still hold 'IMPROVED' on one line (scrollWidth 72). Merged with R2-CODE-10: `.badge` uses `--fs-micro` and the unused `.badge.purple` rule is gone. | fixed |
| R2-VISUAL-11 | all (nav below 900 px) | medium | Mobile/tablet header: the action cluster floats mid-bar and its two 44 px cyan pills carry 9.6 px text | Reproduced (.nav-right 154-285 with a 34px hole before the burger at 375; 44px pills at 9.6px). In the <900px block: nav .nav-right{margin-left:auto}; AI Coach / Log In drawn at 34px with 0.66rem text and a ::before{inset:-5px} hit extender (elementFromPoint 3px above the pill still hits it); #nav-logout-btn min-width 44px. Log In was already a ghost pill via its inline style. After: cluster 168-313 with a 6px gap to the burger at 375 (8px at 768), pills 34px / 10.56px, logout 44x44. | fixed |
| R2-VISUAL-12 | visualizer | medium | 3D lab HUD covers 56 % of the canvas width on phones | Reproduced (HUD 187x101 on a 333x320 canvas). <=480: .viz-overlay-stats becomes a 4-across strip (left/right 8px, gap 4px), .vos-item padding 4px, .vos-val 0.8rem, labels nowrap with 0.5px tracking at the existing 0.6rem floor. After: strip 317x40 (13% of the canvas height), every label fits its tile. | fixed |
| R2-VISUAL-13 | log, ai, analytics, profile | low | Sibling controls in one row differ in height (log date input 42 vs 40, AI send 38 vs input 42 / 48 vs 45, analytics toolbar 30 vs 34, profile row 33) | Reproduced (date 42 vs 40; AI input 42 vs send 38; toolbar 30 vs 34; My PRs buttons 33). .form-select,.form-input{height:40px;line-height:18px} (touch min-height 44 still wins), .ai-input-row{align-items:stretch}, .btn-sm,.btn-ghost-sm{line-height:1.2;min-height:30px}. After at 1280: log row 40/40/40, AI 42/42, analytics toolbar 30/30, My PRs 30 x4, motion prompt + button 40/40. Merged with R2-A11Y-08: `.ai-input-row{align-items:stretch}` (visual) and `.ai-input` on `--border-input` (a11y) both shipped. | fixed |
| R2-VISUAL-14 | ai | low | AI coach page: chat input sits below the fold on a 1280 × 900 desktop and 170 px below it on phones | Reproduced (input row 853-928 on 900px; 748-829 at 375). Under @media(min-height:600px) .ai-wrapper is a flex column capped at calc(100dvh - 148px) (190px at <=480) with .ai-messages{flex:1 1 auto;min-height:180px} giving way first and the header/quick prompts/input row flex-shrink:0; very short viewports keep the flowing layout. After: composer 812-887 at 1280x900 (messages 460), 718-799 at 375x812 (messages 336). | fixed |
| R2-VISUAL-15 | timers (preset "Load" → tempo card), visualizer (analysis output) | low | Programmatic scrolls land the target under the sticky header | Confirmed scroll-margin-top 0 on both targets. html{scroll-padding-top:72px} and #tlt-tempo-card,#viz-output,.tab-panel{scroll-margin-top:72px}. After: computed 72px on #viz-output, #tlt-tempo-card and the html scroll-padding (the hidden pane cannot perform the smooth scroll itself). | fixed |
| R2-VISUAL-16 | planner, home, all (drawer) | low | Orphan tiles: planner summary (2+1 at 375), home footer (2+2+1 at 768), drawer menus (5/3/5 items in 2 columns) | Reproduced (planner third tile 160px alone; footer Compete alone at 768; drawer Learn/Track/Compete lone 151px cards). Added the home rule to the three grids: #page-planner .pl-summary>:last-child:nth-child(odd), .footer-inner>:last-child:nth-child(odd) (<=899 block) and nav .nav-menu>li:last-child:nth-child(odd) all span 1/-1. After: planner tile 335px, footer column 699px at 768, drawer lone cards 308px. | fixed |
| R2-VISUAL-17 | videos, sets, calculator, injury, mental, nutrition, race, taper | low | Tab strips hug the 64 px page title (16 px) on the eight pages without an intro paragraph | Confirmed 16px title-to-tabs on the eight pages. .section-title+.tabs{margin-top:2rem}. After: 32px on videos, sets, calculator, injury, mental, nutrition, race and taper; intro pages unchanged (technique 16px to its intro, zones 20px to its info box). | fixed |
| R2-VISUAL-18 | visualizer | low | Visualizer: the 640 px demo video is left-aligned above an 896 px lab | Confirmed the 640px inline cap on the five demo cards vs the 894px lab. Removed style="max-width:640px" from all five. After: demo card 896px wide at x=190 (504px 16:9 video), the same right edge as the lab (191+894). | fixed |
| R2-VISUAL-19 | visualizer | low | 3D lab control rows wrap raggedly at 375 px | Reproduced (Dolphin Kick and 3/4 View orphaned, speed trio split, 'Skin:' at x=26). Stroke and view rows carry viz-row-strokes / viz-row-view classes; the three speed buttons sit in a .viz-group (display:contents on desktop, role=group 'Playback speed'); the inline 6px on 'Skin:' moved to .viz-btn+.viz-ctl-lbl. <=480: stroke/view buttons 2-up (odd last one full width, 'View:' on its own line), Pause \| Forces share a line, the speed trio fills the next one, label margin reset. After at 375: every row a clean grid, 'Skin:' at x=20; desktop rows unchanged (all five speed-row buttons still on one 34px line). Merged with R2-A11Y-12/16: the stroke row is `.viz-controls.viz-row-strokes` with role="group" aria-label="Stroke" and aria-pressed buttons; the speed trio sits in the `.viz-group` "Playback speed" group; Play/Pause, ♂/♀/🩻 glyphs are aria-hidden spans. | fixed |
| R2-VISUAL-20 | every page except home | low | Sub-page footer is 160 px of padding around one 9.6 px line, with mismatched separator/link sizes | Confirmed 160px sub-page footer and 9.9px separators next to 11.2px links. .page:not(#page-home) footer{padding:1.25rem 2rem} with .footer-bottom{padding-top:0;border-top:0} there; the legal-links <p> now carries class footer-links (static home markup + the enhanceA11y-created one) styled 0.7rem / 0.5px / no uppercase. After: technique footer 79px; links line 11.2px, text-transform none. | fixed |
| R2-VISUAL-21 | home | low | Home hero eyebrow wraps to two lines at 375 px | Reproduced (335x35, two lines at 375). A single line needs <=1.8px tracking at 0.6rem, so <=480 sets .hero-eyebrow{font-size:0.6rem;letter-spacing:1.5px;line-height:1.5;text-wrap:balance}: one 319px line on a 375 phone and balanced (not ragged) lines on anything narrower. After: 1 line, 319x14 at 375. | fixed |
| R2-VISUAL-22 | all (mobile drawer) | low | Drawer header is 56 px while the phone nav bar it slides over is 52 px | Reproduced (head 56 vs bar 52, close at y=6 vs burger y=4). <=480: nav .nav-drawer-head{height:52px;padding-right:12px}. After: head 52px and the close button measures exactly the burger's box (319,4,44,44). | fixed |
| R2-VISUAL-23 | home (feature-card grid, metrics row), any `.reveal` container taller than ~5 viewports | low | Reveal animation waits for 10 % of very tall containers, leaving blank space at the bottom of the phone viewport | Confirmed threshold:0.1 in initReveal on ~860-2400px containers. Observer now uses {threshold:0,rootMargin:'0px 0px -60px 0px'}: a container reveals once its top edge is 60px into the viewport, whatever its height, and a fixed 60px (rather than -10%) can never strand a container at the document end. IntersectionObserver delivers nothing in the hidden pane, so verified by inspection of the options. | fixed |

### Function & data (function) — 15 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R2-FUNCTION-01 | log | medium | Training-log history is ordered by save time, not by session date | Added sortLogEntries (date desc, then id desc); saveLogEntry sorts before writing (new entry prepended so an equal-date tie keeps the newest save first) and renderLog sorts for display, so imported and after-the-fact sessions sit where they happened. Verified: 09-15, 09-01, 09-17 saves render 09-17, 09-15, 09-01. | fixed |
| R2-FUNCTION-02 | profile (My PRs › Import Backup) | medium | Import Backup silently replaces the local log, PRs and name with no confirmation, and an empty name in the file wipes the saved name | importBackup now confirm()s before writing when the device holds data the file will overwrite, naming only what that file carries ('the 3 sessions, 2 PRs and saved settings already on this device'); declining leaves everything untouched with an 'Import cancelled' notice; swimmer_name is written only when d.name.trim() is non-empty. Verified: log:[] on a 3-session device is declined/kept; name:'' keeps 'Dan'; a name-only file asks nothing. | fixed |
| R2-FUNCTION-03 | every page (Log in / Sign up overlay, AI gates) | medium | Auth dialog: closing then re-opening within 450 ms makes the re-opened dialog vanish; the invisible overlay eats clicks and blocks the palette during the fade | closeAuthGate stores its hide timer, adds a .closing class and pointer-events:none immediately; openAuthGate clears the timer, removes .closing, resets transition/opacity. New authGateVisible() drives _authTrap, and the palette's authGateOpen() treats .closing as closed. Verified: close then re-open at +100 ms is still visible at +550 ms (signup screen shown); during the fade elementFromPoint no longer hits the overlay and openPalette() opens. Merged with R2-CODE-02: openAuthGate first defers until the overlay markup exists (code), then clears the hide timer / .closing (function). | fixed |
| R2-FUNCTION-04 | log | medium | 30-day summary keeps showing stale numbers once no session falls in the last 30 days | renderLog hides #log-stats (else branch) when entries exist but none fall in the last 30 days, matching the empty-log behaviour. Verified: one 2026-05-01 session leaves the panel display:none instead of the stale '3 sessions · 12.0 km'. | fixed |
| R2-FUNCTION-05 | calculator (all four tabs) | low | Calculator page keeps the previous result table on screen when a new run errors (Golf, Splits, Pace Zones, Converter) | Added calcFail(noticeId,msg,focusId,resultId) with CALC_PLACEHOLDERS: resets #split-result/#pace-result/#conv-result to their placeholder and removes .show from #golf-result before the notice; used on every early return in calcGolf/calcSplits (incl. the planner-unavailable path)/calcPaceZones/convertTime. Verified: blank after a valid run leaves 0 rows and the placeholder text; golf result loses .show. Merged with R2-A11Y-05: calcFail now calls tlNotice(noticeId,msg,null,focusId) so the offending field also gets aria-invalid + aria-describedby. | fixed |
| R2-FUNCTION-06 | calculator, log, analytics | low | Enter does nothing in the Calculator, Training Log and Analytics forms (the Planner, auth and AI forms all submit on Enter) | One delegated keydown listener: plain Enter in a text/number/date input inside .calc-panel or the Log's .tl-panel (not in a <form>, not defaultPrevented, no modifiers) clicks the panel's .calc-btn or .btn-sm. Covers Calculator, Log, Analytics, Timers' interval panel and the planner fields without their own handler; planner fields with handlers (preventDefault) do not double-fire; textarea and Shift+Enter untouched. Verified in the browser for split-time, log-distance, an-sw-time, pl-sp-goal (single run), pl-sp-fade, tlt-so-reps. Merged with R2-A11Y-20 in the post-merge fix: the two delegated Enter handlers were collapsed into one top-level listener covering `.calc-panel`, `.gen-form` (#gen-btn) and the Log panel. | fixed |
| R2-FUNCTION-07 | profile | low | PR fields flag "invalid" on the first keystroke and store half-typed values | savePR keeps a per-key timer (prErrTimers): on input both the invalid flag/hint and the store of a valid value wait for an 800 ms typing pause; change (blur) flushes at once with normalisation; a value that becomes valid clears a previous error immediately. Verified: typing 1,1:,1:5,1:52,1:52.,1:52.3 never sets aria-invalid or stores an intermediate; after the pause '1:52.3' is stored; change normalises to 1:52.30; 'abc' flags after 800 ms. | fixed |
| R2-FUNCTION-08 | timers (Tempo Trainer) | low | Tempo trainer: toggling strokes/min → cycles/min → strokes/min changes the tempo by one | tlt_setMode remembers the pre-conversion tempo (TP.undo = {mode, bpm, at}) and restores it when switching straight back with no edit in between. Verified: 65 spm → 33 cpm → 65 spm; after editing to 34 cpm the switch back gives 68 (no stale restore). | fixed |
| R2-FUNCTION-09 | ai | low | AI Coach stacks identical bubbles: every logged-out Send adds another "needs a free account" message, and clicking the current mode again adds another "Mode: …" message | setMode returns early when the mode is already current; sendToAI's logged-out branch skips the gate bubble when the last AI bubble already is that text (lastAIBubbleText helper). Verified: 3 Sends logged out add 1 bubble; clicking the active chip twice adds 1 'Mode:' line; switching modes still announces. | fixed |
| R2-FUNCTION-10 | technique (Ask the Visualizer form) | low | Technique › "Ask the Visualizer": the "Custom Description (optional)" text is ignored unless the concept dropdown is set to Custom, and it has no length cap | Prompt topic is the preset plus ' — focus: <text>' when text is given (Custom still uses the text alone); textarea gets maxlength=300 and the value is sliced to 300; label reads 'Extra focus (optional) — or your own concept for Custom'. Verified from the built function source and DOM. | fixed |
| R2-FUNCTION-11 | log (and Import Backup) | low | Training log accepts sessions dated in the future, and the backup sanitiser accepts impossible dates like 2026-02-31 | Added parseLogDate (strict round-trip, same rule as planner/analytics) and logTodayISO(offset); saveLogEntry rejects unparseable dates and anything later than tomorrow with an inline message; sanitizeLogEntries applies the same rule to backups; #log-date gets max=tomorrow. Verified: 2027-01-01 and 2026-02-31 are refused in the form and dropped by the sanitiser; today and tomorrow save. | fixed |
| R2-FUNCTION-12 | calculator (Golf Score) | low | Golf-score inputs' HTML min/max disagree with the JavaScript limits | GOLF_LIMITS {split:[20,120], strokes:[10,60]} now drives calcGolf's range checks and messages, matching the existing min/max attributes. Verified: 16 s / 8 strokes now yields '50m split should be between 20 and 120 seconds.' and no stale score. Merged with R2-A11Y-05 (field flag via calcFail) and R2-COPY-15 ("50 m" spacing in the messages). | fixed |
| R2-FUNCTION-13 | visualizer | low | 3D lab view presets disable orbit controls until a requestAnimationFrame tween finishes; a hidden tab or a second click mid-tween leaves controls off or two tweens fighting, and reduced-motion is ignored | setView3D keeps one _viewTween3D handle: cancelViewTween3D(finish) cancels a running tween (a second preset click replaces the first), document.hidden or prefers-reduced-motion jumps straight to the end pose with controls enabled, a tween that notices the tab went hidden finishes itself, and stopViz3DLoop finishes any running tween. Verified with the pane hidden (camera at the top pose, controls enabled) and with stubbed rAF/hidden for the two-click visible path. Merged with R2-CODE-08: stopViz3DLoop also cancels a running view tween; vizLabActive() gates the router callback and startViz3D. | fixed |
| R2-FUNCTION-14 | updates | low | "Check for updates" blames https/browser support when the service worker simply failed to register on a supported host | New swFailed flag set in registerSW's catch paths; tlCheckForUpdates and the offline note show 'Offline support could not be set up in this browser session — reload to try again; the site still works online.' when registration failed on a supported host, keeping the https/browser-support wording only for unsupported hosts. Verified on the served localhost copy where sw.js registration fails. Merged with R2-CODE-17: the function lens's SW_FAILED_MSG constant is used by both the status card and the update check; the code lens's console.warn with the error is kept. | fixed |
| R2-FUNCTION-15 | log | low | Log form's default date is computed once at load and goes stale after midnight | setLogDateToday() fills #log-date with today's local date only when the field is empty or still holds the previous auto value (a typed date is kept) and sets max; called at load, from a new window.onShow_log hook and on visibilitychange (visible). Verified: user date 2026-09-10 kept; empty refilled; a stale auto value is replaced. | fixed |

### Accessibility (a11y) — 22 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R2-A11Y-01 | all (Log In / Sign Up / Reset dialog) | high | Regression of R1-A11Y-21 / R1-VISUAL-07: the auth-gate close button is unreachable by keyboard | _authTrap now filters focusables with !disabled && !hidden && getClientRects().length>0 (the fixed-position close button has no offsetParent); the close button is the first trap stop. Verified: Shift+Tab from #login-email lands on 'Close sign-in dialog', Shift+Tab again wraps to 'Create an account'. | fixed |
| R2-A11Y-02 | analytics (Stroke rate & DPS tab), planner (Race Predictor tab) | high | Phone layout zooms out to half size on Analytics › Stroke rate & DPS and pans on Planner › Race Predictor | .calc-panel, .form-group and .form-input/.form-select/.form-textarea get min-width:0; .gen-row tracks are minmax(min(190px,100%),1fr). Verified at 375px: Analytics Stroke rate innerWidth 751->375, panels 731->335; Planner Race Predictor 391->375, panels 370->335; every page and tab fits at 375 and 320. See R2-COPY-01 for the merged CSS. | fixed |
| R2-A11Y-03 | timers (Tempo Trainer, elite stroke-rate table) | medium | Timers page pans 21 px sideways on phones because a visually-hidden table header escapes the scroll container | .table-wrap is position:relative and the hidden 'Load into the tempo trainer' header uses the shared .sr-only class. Verified at 375px: timers scrollWidth 396->375. See R2-COPY-08 for the merged CSS. | fixed |
| R2-A11Y-04 | ai, workouts, visualizer (Ask the Visualizer) | medium | Streamed AI text is re-announced on every frame and the finished answer / errors are never announced | Stream targets are aria-live=off with aria-busy on the bubble/#gen-result/#viz-content while streaming; the chat bubble drops aria-live/aria-busy right before the final text lands so the polite log announces the reply (or error) once; new sr-only #tl-ai-status + tlAnnounce() announces 'Workout ready.', 'Generating analysis...', 'Analysis complete.', session-expired, HTTP/rate-limit and connection errors; #gen-loading is role=status. | fixed |
| R2-A11Y-05 | calculator (Golf Score, Race Splits, Pace Zones, Time Converter), visualizer (Ask the Visualizer) | medium | Calculator validation and results are invisible to screen readers | tlNotice takes an optional field arg and sets aria-invalid=true + aria-describedby=<notice id> on it (cleared on the next call); all Golf/Splits/Pace/Converter and Ask-the-Visualizer validations pass the offending field; #golf-result (atomic), #split-result, #pace-result, #conv-result are aria-live=polite. Verified in browser. Merged with R2-FUNCTION-05/12: calcFail forwards the field id to tlNotice. | fixed |
| R2-A11Y-06 | timers | medium | Timers page hijacks Space and the arrow keys while focus is on the page body | Tempo card, interval display and split-taker card are tabindex=0 role=group with labels (and a focus-visible ring); the timers onKey handles Space/arrows only when focus is inside the active tab's card or that timer is already running; hint texts updated. Verified: on <body> ArrowDown/Space are no longer prevented and the tempo stays 60; with the card focused ArrowDown nudges 60->59. | fixed |
| R2-A11Y-07 | technique (IM), calculator (Time Converter), race (Pacing Science), taper (Recovery), timers, planner, analytics, season, nutrition and every `.info-box`/`.pl-hint`/`.tlt-note` link | medium | Inline text links are distinguished by colour alone (1.01:1 against the surrounding text) | Zero-specificity :where() rule underlines links inside main p/li/td/dd, .info-box, .warn-box, .pl-hint, .pl-fine, .tlt-note, .an-hint (button-styled classes excluded); main footer a stays undecorated. Verified: prose links underline, nav/footer none; What's New item links keep their existing ' ->' cue. | fixed |
| R2-A11Y-08 | all forms (calculator, log, profile, planner, analytics, timers, workouts, AI input, auth gate) | medium | Text-field boundaries are 1.4:1 — inputs are not perceivable as fields | New --border-input:rgba(0,212,255,0.5) used by .form-input/.form-select/.form-textarea, .ai-input, .pr-input and .pl-goal-input. Measured: 3.46:1 outside / 3.35:1 inside on the log fields (was 1.38/1.35). Merged with R2-CODE-15: `--border-input` and the Bebas fallback font stack coexist in :root. | fixed |
| R2-A11Y-09 | planner (Goal Times, Meet Countdown) | medium | Planner Goal Times: 18 fields named "Goal time", 18 links "No PR — add one", 18 buttons "Plan splits →" | renderGoalCard: card is role=group aria-labelledby=<event span id>, label reads 'Goal time (for 50 FR)' via sr-only, quick-fill group/buttons, 'No PR for 50 FR - add one' link and 'Plan splits for 50 FR' button include the event; Meet Countdown labels are 'Next meet name'/'First day of the next meet'/'Second meet name'/'First day of the second meet'. Verified: zero duplicate names on #page-planner. | fixed |
| R2-A11Y-10 | timers (Tempo Trainer) | medium | Eleven "Load" buttons in the elite stroke-rate table have no event in their name | renderEliteRateTables emits aria-label="Load <event> race tempo" on each Load button (visible text unchanged). Verified: 11 distinct names. | fixed |
| R2-A11Y-11 | ai | medium | Regression of R1-A11Y-10: "Clear" leaves the AI mode buttons' `aria-pressed` out of sync | clearChat resets aria-pressed=false on every mode button and true on #mode-coach. Verified: after Race then Clear, coach=true, race=false. | fixed |
| R2-A11Y-12 | visualizer | medium | 3D-lab stroke buttons and "Forces" toggle expose no pressed state | Five stroke buttons carry aria-pressed inside a role=group aria-label='Stroke'; setStroke3D maintains it; #vbtn-forces has aria-pressed toggled in toggleForces3D. Verified. See R2-VISUAL-19 for the merged markup. | fixed |
| R2-A11Y-13 | nutrition, race, taper, mental, injury, season | medium | 320 px reflow: `.grid-2` cards are 320 px wide in a 280 px column | .grid-2 minmax(min(320px,100%),1fr) and .grid-3 minmax(min(280px,100%),1fr). Verified at 320px: nutrition/race/taper/injury/season scrollWidth 340->320; all 22 pages fit. | fixed |
| R2-A11Y-14 | log, timers, profile, planner (Goal Times), analytics (empty state), visualizer (info panel) | medium | Sub-section titles on Log, Timers, Profile, Planner and Analytics are styled divs, not headings | Log's three titles, Timers' CIRCUIT SETUP/PRESETS and the three eyebrow section titles, Profile's five stroke groups (plus the Swimmer name heading h3->h2 so the outline has no gap), Planner's renderGoals group titles, Analytics' empty-state title and the visualizer phase title are h2 (font-weight:400 keeps the look). The elite-rate table is now named 'Elite race stroke-rate ranges'. Verified: no skipped heading level on any page. | fixed |
| R2-A11Y-15 | profile | low | Swimmer Name field has no `autocomplete` token (WCAG 1.3.5) | #profile-name has autocomplete="name". | fixed |
| R2-A11Y-16 | auth gate, profile, workouts, visualizer, calculator results | low | Remaining decorative glyphs are read aloud (auth cards, backup buttons, 3D-lab buttons, generator buttons) | Glyphs wrapped in aria-hidden spans: auth-card swimmer/key divs, backup arrows, print/regenerate glyphs, male/female/x-ray glyphs, Play/Pause (togglePlay3D and the reduced-motion path rebuild static markup with a hidden span), the sparkle on both Generate buttons, the mouse glyph in the lab hint, and the Golf rating glyph (built with createElement/textContent). See R2-VISUAL-19 for the merged markup. | fixed |
| R2-A11Y-17 | visualizer | low | The focusable 3D canvas is `role="img"`, which hides its keyboard interaction | #viz3d-container is role="application" with a label that names the Left/Right arrow-key phase stepping. | fixed |
| R2-A11Y-18 | all | low | "/" opens the search palette from anywhere with no way to disable it | Persisted toggle in the palette footer ('Slash key / also opens search', tl_pal_slash, default on) checked by onGlobalKey via slashEnabled(); footer hints stay aria-hidden individually; the dialog Tab cycle is input -> close -> toggle (when shown) -> input; the change is announced through the palette status. Verified: with the toggle off '/' is not intercepted and Cmd/Ctrl+K still opens the palette. | fixed |
| R2-A11Y-19 | planner (Race Predictor exponent table, Split Planner pattern table), season | low | Dynamically rendered tables miss the naming pass; the season table is named after the page | sr-only captions on the planner exponent and pattern tables (plus the prediction, planned-splits and the three calculator result tables); #season-meet-table is aria-labelledby=season-calendar-eyebrow ('2026-27 National Meet Calendar'). See R2-VISUAL-06 for the season table; the calculator result tables carry both the sr-only captions and the copy lens's `.th-unit` spans. | fixed |
| R2-A11Y-20 | calculator (4 tabs), analytics (SWOLF, Stroke rate, CSS), timers (Send-off), workouts (notes) | low | Enter does not submit the calculator, analytics-calculator or send-off forms | One delegated keydown handler in enhanceA11y: Enter in a text-like input inside .calc-panel or .gen-form clicks that panel's .calc-btn/#gen-btn unless the field already handled Enter (defaultPrevented). Verified: Enter in #golf-strokes computes the score. See R2-FUNCTION-06 (single handler after the post-merge fix). | fixed |
| R2-A11Y-21 | workouts, visualizer (Ask the Visualizer) | low | Generated workout / analysis headings skip from level 1 to level 4 | formatWorkout emits <h2> per section (CSS .workout-phase h2,h4 + print rule) and formatVizOutput emits <h3> section titles (font-weight:400 keeps the look) under the page's h2 'ASK THE VISUALIZER'. | fixed |
| R2-A11Y-22 | visualizer (Ask the Visualizer) | low | One smooth scroll ignores `prefers-reduced-motion` | Analysis-complete scroll uses behavior:reducedMotion()?'auto':'smooth'. | fixed |

### Code quality, performance & SEO (code) — 17 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R2-CODE-01 | SEO | high | sitemap.xml is not well-formed XML (regression of R1-CODE-17 / R1-COPY-15) | sitemap.xml: rewrote the round-1 comment without any '--' (points at README 'Releasing a new version' instead of quoting the git command); xmllint --noout and minidom now parse it. README release step 4 tells the releaser to run xmllint --noout sitemap.xml. | fixed |
| R2-CODE-02 | all (first load) | high | The deferred supabase-js script gates the app's first paint on a third-party CDN | supabase-js now loads with async (integrity/crossorigin kept) and reports through onload/onerror -> onSupabaseScript(), which creates the client as soon as the library lands (or on DOMContentLoaded, whichever is later); openAuthGate() queues a Log In click while the script is still in flight instead of alerting, and waits for the overlay markup if clicked mid-parse. Before/after with a 4 s stub CDN: domContentLoadedEventStart 4020 ms -> 27 ms (domInteractive 24 ms), the routed page paints immediately. See R2-FUNCTION-03 for the openAuthGate order. | fixed |
| R2-CODE-03 | auth overlay / router | high | Password-reset (and every other auth-callback) link is dead: the router strips the token hash before supabase-js can read it, and no recovery UI exists | Router keeps auth-callback hashes (isAuthCallbackHash: access_token/refresh_token/error*/type) so supabase-js can read them; initSupabaseAuth subscribes onAuthStateChange and PASSWORD_RECOVERY opens a new screen-newpass card (password + confirm, 8+ chars, sb.auth.updateUser, then signed-in close); #error_description=... opens the login/forgot card with the decoded message; a rejected recovery token opens the forgot card with 'could not be verified'; the URL is cleaned in every branch and onAuthSuccess no longer closes the overlay while recovery is pending. Verified: the fake recovery hash now reaches /auth/v1/user (previously zero requests), expired-link hash shows the message, simulated PASSWORD_RECOVERY opens the new-password screen with focus in the field. Owner still has to confirm the site URL is in the Supabase redirect allow-list (backend read-only). | fixed |
| R2-CODE-04 | visualizer | medium | three.min.js (603 KB) is injected from jsDelivr with no integrity hash or crossorigin | ensureViz3DReady sets s.crossOrigin='anonymous' and s.integrity='sha384-CI3ELBVUz9XQO+97x6nwMDPosPR5XvsxW2ua7N1Xeygeh1IxtgqtCkGfQY9WWdHu' (hash recomputed from the fetched 603,445-byte file); the lab loaded, executed and rendered under it in the browser. | fixed |
| R2-CODE-05 | all | medium | No Content-Security-Policy at all | Added <meta http-equiv=Content-Security-Policy> (default-src 'self'; script-src self+unsafe-inline+cdn.jsdelivr.net; style-src self+unsafe-inline+fonts.googleapis.com; font-src self+fonts.gstatic.com; img-src self data: i.ytimg.com; connect-src self+the Supabase project; frame-src youtube-nocookie; worker-src/manifest-src self; object-src none; base-uri/form-action self). Smoke-tested: Google Fonts, supabase-js + Three.js from jsDelivr, Supabase connect (auth health 200), YouTube thumbnail + facade iframe, WebGL lab render, blob backup download, palette, every route; a fetch to example.com is refused. | fixed |
| R2-CODE-06 | PWA | medium | sw.js precaches viz-human.js (330 KB) for a 3D lab that can never run offline, plus an asset nothing references | sw.js: logo-full.svg dropped from PRECACHE and the unreferenced file deleted (tools/make_logo.py can regenerate it); favicon.ico added; the pinned three.min.js is now served cache-first from a separate 'tl-vendor-cache' that survives version sweeps (stale entries for other Three builds are swept on activate), so the 3D lab works offline once opened; viz-human.js stays precached so the lab is offline-capable after an update too. What's New status copy updated accordingly. A Node harness verified cache-first, offline serving, opaque responses uncached, other CDN URLs untouched and the activate sweep keeping the vendor cache (the harness caught and fixed a prefix collision with 'tidelyne-v'). | fixed |
| R2-CODE-07 | PWA / updates | low | Update flow: skipWaiting + clients.claim() lets the old page load the new viz-human.js | Page-side guard (option 2): the updates module sets window.TL_SHELL_STALE when the controlling worker's version is newer than window.TL_VERSION (markShellStale on TL_UPDATED/TL_VERSION) and exposes window.tlShowUpdateToast(note); loadHumanScripts then shows the mannequin and the toast 'Tidelyne updated to vX — reload to open the updated 3D lab' instead of injecting a module built for the newer shell. Verified by simulation (no script injected, toast shown). | fixed |
| R2-CODE-08 | visualizer → any | low | Visualizer render loop starts (and keeps running) on other pages when the lab is left within 30 ms or while Three.js is still downloading | Added vizLabActive(); the router's 30 ms callback returns early when the lab is no longer active, and startViz3D builds the scene but only calls animate3D() when the lab is on screen (the router starts the loop on the next visit). Verified: quick hop -> _raf3D 0 and no Three.js download; leaving during the download -> scene initialised with _raf3D 0; returning starts the loop, leaving stops it. See R2-FUNCTION-13. | fixed |
| R2-CODE-09 | profile (My PRs) | low | Backup/PR/profile code still reads localStorage raw, bypassing the lsGet helper R1-CODE-12 added | buildPRGrid, exportBackup, initProfile and the palette's readRecent read through lsGet; exportBackup's whole body is inside its try so a storage failure surfaces via tlNotice. Verified with Storage.prototype.getItem throwing: initProfile and exportBackup no longer throw and the PR grid still builds. | fixed |
| R2-CODE-10 | n/a | low | Dead code and overridden rules left in the page | Removed seriesLabel, the .goal-input selector and .badge.purple; moved #8ba5bd/#7b95ad into the primary :root and deleted the override block; reduced the two IIFE shims to lsPut(k,v){return !!window.lsSet(k,v)} / lsDel(k){window.lsRemove(k)}. See R2-VISUAL-10. | fixed |
| R2-CODE-11 | visualizer | low | R1-CODE-11 leftover: a comment still cites the non-existent preview/NOTES.md | viz-human.js comment now reads '(index.html updateSwimmer; see README.md "Maintainer notes")' — no motion values touched; grep NOTES.md finds nothing. See R2-COPY-24. | fixed |
| R2-CODE-12 | privacy, terms, disclaimer, 404 | low | Legal pages and 404 still load Google Fonts render-blocking (R1-CODE-04 fixed index.html only) | privacy/terms/disclaimer/404 use the preload-and-promote pattern with a <noscript> fallback, and request only DM Sans 400;600 (the weights the pages use; <strong> resolves to 600 as before). Verified the link is promoted to rel=stylesheet and Bebas Neue / DM Sans 400+600 load. | fixed |
| R2-CODE-13 | all | low | No /favicon.ico | Generated favicon.ico (32 px + 16 px classic BMP entries from the same mark), taught tools/make_pwa_icons.py to regenerate it, added <link rel=icon href=favicon.ico sizes=32x32> (listed first so browsers keep the SVG/PNG they show today) to index.html and the legal pages, the /JAC_Online/ form on 404.html whose ROOT rewrite now covers every icon link, and added it to the SW PRECACHE. Note: on GitHub Pages the origin-root /favicon.ico is outside this repo; the file answers /JAC_Online/favicon.ico and the link tag. | fixed |
| R2-CODE-14 | SEO / hygiene | low | Backend sources, tool scripts and (after the next push) the audit log are published on the public site | robots.txt now disallows /supabase/, /tools/, /README.md and /LICENSE (with a comment); README's file table says the folders are published but disallowed. The longer-term Actions/docs/ deploy change was not made (out of scope). | fixed |
| R2-CODE-15 | all | low | Bebas Neue swaps in with no size-adjusted fallback, so the hero and every section title reflow on font arrival | Three metric-matched @font-face fallbacks (Bebas Fallback: Arial Narrow 71%; Bebas Fallback Impact: Impact 76%; Bebas Fallback Sans: Arial/Roboto/Helvetica 59%; all ascent 90% / descent 30% / line-gap 0) chained in --fh before sans-serif. Measured in the browser: sample width 1244 px vs 1245 px with Bebas Neue (plain sans-serif: 1998 px); at 375 px the hero h1 stays 3 lines and the whole Home page height is identical (5733 px) with the fallback and with Bebas Neue, versus 5894 px with plain sans-serif. See R2-A11Y-08. | fixed |
| R2-CODE-16 | all hash routes | low | Route titles omit the brand and two exceed 60 characters | setPageTitle appends ' — Tidelyne' to every non-home route; shortened calculator, planner, technique, sets, timers and analytics titles and dropped the duplicated brand from the updates entry so every full title is <= 60 characters (longest now exactly 60). Verified titles on 6 routes. See R2-COPY-07: the copy lens's setPageTitle rule shipped; the shortened PAGE_TITLES entries and the de-branded updates title are kept. | fixed |
| R2-CODE-17 | updates | low | What's New blames "needs https / no service-worker support" when registration itself failed | Separate swFailed flag set in register().catch (and the outer catch, with a console.warn carrying the error); the status card now says 'Offline support could not be set up in this browser (the service worker failed to install). Reload to retry; the site still works online.' and tlCheckForUpdates has a matching message, reserving the https/support sentence for !canRegister(). Verified in the pane, where registration is refused. See R2-FUNCTION-14. | fixed |

### Swimming domain accuracy (domain) — 18 findings

| ID | Page | Severity | Finding | Fix applied | Status |
|---|---|---|---|---|---|
| R2-DOMAIN-01 | visualizer | medium | Freestyle and backstroke are authored with true opposition timing (R[i] = mirror of L[i+3]), so phases i and i+3 are the SAME body configuration mirrored — yet they carry different physics. Measured on the human rig: freestyle phase 3 "Pull" has the L hand at (0.15, 0.14, −0.30) m mid-pull and phase 6 "Reach Forward" has the R hand at (0.15, 0.19, +0.35) — identical — but the panel reads **142 N thrust / 2.18 m/s / net +76** vs **24 N / 2.02 / net −6**. Backstroke phase 1 "Pinky Entry" (R hand finishing at the hip, (−0.04, 0.19, −0.44)) reads **11 N / 1.55 m/s / −12** while its mirror phase 4 "In-Sweep to Hip" reads **130 N / 2.05 / +68**. The description of phase 6 even says "the opposite arm drives through the middle of its pull" while the thrust box shows 24 N. A coach stepping through the phases sees the swimmer slow from 2.05 to 1.55 m/s twice a cycle in backstroke for no visible reason. | index.html STROKES: freestyle pairs (0,3)/(1,4)/(2,5) now carry identical physics 44/36/16/1.94/66, 40/28/24/1.84/62, 60/80/40/1.80/74 (weighted net +2.1 N); backstroke pairs 40/34/12/1.86/62, 38/24/20/1.76/58, 56/78/30/1.72/68 (+2.6 N). Verified on the human rig: fs phase 3 L hand (0.15,0.14,-0.30) and phase 6 R hand (0.15,0.19,0.35) both read 80 N / 60 N / +20 / 1.80; bk phases 1 and 4 both 34/40/-6/1.86. 'In-Sweep to Hip' desc no longer claims peak thrust (it is the finish, mirror of the entry). See R2-COPY-16. | fixed |
| R2-DOMAIN-02 | technique (Starts) | medium | the two start angles are inverted. Elite track/kick starts leave the block with the centre-of-mass velocity vector close to horizontal (roughly −5° to +10°; Vantorre, Chollet & Seifert 2014; Tor, Pease & Ball 2015) because the block is 0.5–0.75 m above the water — 35–45° is the projectile-range optimum for a launch at water level and would produce a high, slow dive. The ENTRY angle is the steep one: elite entries are about 30–45° to the surface, not 15–20° (a 15–20° entry is a flat "belly" dive that costs speed). | Starts card: take-off 'near horizontal — about 0–10° above the horizontal; the 0.5–0.75 m block height gives the flight'; entry 'about 30–40° to the surface — hands, head, hips and feet through one hole'; the paragraph's 'Takeoff angle: 35–45°' updated to match. Shipped with the copy lens's SI spacing ("0.5–0.75 m", "10–15 m"). | fixed |
| R2-DOMAIN-03 | technique (Backstroke) vs injury (Neck) | medium | the technique page's backstroke body-position card tells the swimmer to look "toward feet", while the Injury › Neck tab lists "chronic chin-tuck looking at the feet" as a cause of neck pain and says "keep the neck neutral". The two pages contradict each other, and the technique cue is the one that is wrong: the head is held still with the eyes up (toward the ceiling/flags), chin neutral; tucking the chin to look at the feet lifts the head, drops the hips and loads the neck. | Backstroke body-position card: 'eyes up toward the ceiling with the chin neutral (look at the flags, not at your feet — a chin-tuck lifts the head, drops the hips and loads the neck)', now consistent with Injury › Neck. | fixed |
| R2-DOMAIN-04 | visualizer | medium | (a) R1-DOMAIN-09 established that a steady-state stroke must average net ≈ 0 N over the cycle and fixed freestyle only. The dolphin kick still averages **+50 N** (thrust exceeds drag in 4 of 5 phases: −24, +80, +108, +51, +26 weighted by duration) while its velocity ends the cycle where it started; butterfly averages +24 N, backstroke +22 N, freestyle +19 N (breaststroke +6 N is fine). (b) The velocity trace contradicts the sign of the net force at several keyframe transitions: breaststroke "Streamline Glide" (net −14 N) → "Glide & Streamline" (net −12 N) SPEEDS UP 1.72 → 1.80 m/s in a passive glide; butterfly "Recovery" (−18 N) → "Entry" (−10 N) → "Press" rises 1.66 → 1.70 → 1.72; dolphin "Upbeat Drive" (+51 N) → "Upbeat Peak" (+26 N) → "Heels Up" falls 2.15 → 2.10 → 2.05. The panel is labelled "illustrative", but the one lesson it exists to teach (you decelerate in the glide) is inverted for breaststroke. | viz-human.js STROKE_PATCHES re-keyed so every phase's velocity moves in the direction of its keyframe net force and the duration-weighted net is near zero: breaststroke +2.4 N (glide decays 1.86 → 1.68 → 1.50), butterfly +3.4 N (Recovery 1.70 → Entry 1.62 → Press 1.70 → Catch 1.66 → Keyhole 1.84 → Push 2.04 → Exit 2.10), dolphin +3.8 N (2.10 → 2.00 → 2.14 → 2.22 → 2.24, drag 48/56/70/54/56, thrust 12/100/104/58/12). The mannequin fallback records in index.html were re-keyed the same way (breast +3.5, fly -2.3, dolphin +2.0 N; sign check passes on all five). Verified live: 0 sign mismatches in all strokes after applyPatches. | fixed |
| R2-DOMAIN-05 | workouts | medium | the prompt tells the model "Volume targets for this level: 45min=…, 60min=…, 75min=…, 90min=…" but the Session Length select also offers **120 min** ("2 hours", value 120) — for that choice no target exists and the model is left to guess. The same target string is sent regardless of Meso Phase and Focus, so "Taper / Pre-Meet" or "Recovery / Easy" sessions are still asked for full base volume (senior 60 min = 3,500–4,500 m), contradicting the Taper page (volume down 30–70 %) that the site says its prompts follow. | VOL_TARGETS is now a per-length table with 120 min for every level (age-group/masters 5000–7000, senior 6000–8500, elite 7000–10000); taper meso scales the target ×0.5–0.6 with 'total volume cut 40-60% from base — keep the race-pace quality short and sharp with full rest', Recovery/Easy focus ×0.7, Speed focus 'keep total volume at the low end'. Verified with a stubbed session/fetch (no live call): 120-min senior taper → '3000-5100m … (taper: …)'; 120-min elite recovery → '4900-7000m'. | fixed |
| R2-DOMAIN-06 | technique (Underwater) + visualizer (AI concept) | medium | the site's own research note in viz-human.js (dolphin patch `sources`) cites elite toe-to-toe amplitude **0.45 ± 0.06 m** (PMC7739797) and "toe amplitude 0.45–0.6 m" for butterfly; the Technique page and the "Ask the Visualizer" prompt say 25–35 cm — roughly half. The prompt propagates the wrong number into every AI analysis of the dolphin kick. | Technique › Underwater key-point now 'Kick amplitude: About 45–55cm toe-to-toe at the feet (the hips move only ~15cm). Hip-driven.'; VIZ_CONCEPTS.dolphin-kick prompt says '~0.45-0.55m toe-to-toe at the feet, hips ~0.15m' and adds the ~1.9 Hz elite mean rate. Shipped with the copy lens's SI spacing ("45–55 cm", "~15 cm"). | fixed |
| R2-DOMAIN-07 | nutrition (Daily Fueling) | medium | the carbohydrate ladder puts taper weeks (7–8 g/kg) ABOVE base training (6–7 g/kg) although taper volume drops 30–70 %; energy and carbohydrate needs fall with the volume until the 1–2 day pre-race load (which the Race Day tab already covers at 8–10 g/kg). As written, a swimmer eating 7–8 g/kg through a two-week taper is being told to over-fuel. | Carbohydrate ladder: 'Taper weeks: 5–6 g/kg as volume drops (weight stays stable) — rising to 8–10 g/kg only on the 1–2 days before racing'. | fixed |
| R2-DOMAIN-08 | race (Pacing Science) | low | the planner's own PATTERNS give 200 FLY halves 47.7 / 52.3 % (second 100 **9.6 %** slower — three times the freestyle fade of 2.8 %) and 200 BR 48.6 / 51.4 % (5.8 %). "A little more" understates a fade of ~10 s in a 1:50 fly. | Pacing Science: 'butterfly and breaststroke 200s fade more — the second 100 is typically 5–6% slower in breaststroke and 8–10% slower in butterfly, so holding the third 50 is a stretch target there' (matches PATTERNS 200 BR 48.6/51.4 and 200 FLY 47.7/52.3). Shipped with the copy lens's "100 m / 200 m / 400 m" spacing and US "minimize". | fixed |
| R2-DOMAIN-09 | race (Warm-Up) | low | the protocol adds up to 400–600 + 200 + 200 + 100 + 30 + 200 = **1,130–1,330 m**; the box still overstates by ~70 m at both ends (R1-COPY-13 narrowed it from 1,200–1,800 but did not make it add up). | Warm-up box now 'Total: about 1,150–1,350m' = 400–600 + 200 + 200 + 100 + 30 + 200. Shipped as "about 1,150–1,350 m" (copy lens spacing). | fixed |
| R2-DOMAIN-10 | technique (Underwater) | low | the merged R1 sentence contradicts itself: "About 2.0–2.5 Hz (120–150 kicks/min) at full race speed; elite means sit near 1.9 Hz." If the elite mean at maximal effort is 1.9 Hz (the cited PMC7739797 figure), 2.0–2.5 Hz is not the typical full-race-speed rate but the top of the distribution. | Elite rate key-point: 'About 1.9 Hz on average at race speed (≈115 kicks/min), with the fastest sprinters at 2.0–2.5 Hz (120–150). Rate, not amplitude, is what rises with speed.' See R2-COPY-17. | fixed |
| R2-DOMAIN-11 | calculator (Race Splits) | low | the plausibility check fires only below 15.0 s per 50, but the 50 m world record is 20.91 s: 100 m in 30.5 (14.55 / 15.95 splits, verified) or 400 in 2:30 sail through with a message that names the world record. Also the generator has no course selector and always applies the long-course dive bonus ("−1.4 s, long course") to swimmers who may be planning a short-course race. | calcSplits sanity check fires below 19 s per 50 ('faster than any 50m ever swum'); the caption now reads 'First 50 includes the dive (−1.4 s) · Long-course pattern — use the Race Planner for short course' with a link to the planner's split tab. Verified: 100 in 30.5 now shows the notice; 200 in 1:55 renders with the new caption. Merged with R2-FUNCTION-05 and R2-COPY-15: the check runs through calcFail (result placeholder restored, field flagged) with the 19 s threshold and "per 50 m" wording; the caption keeps the Race Planner link. | fixed |
| R2-DOMAIN-12 | analytics (Intensity distribution) | low | the reading compares Z1–Z2 minutes with Hellard's "below 2 mmol/L" share and Z3 with "2–4 mmol/L", but the site's own zone definitions put Z2 AeT at **2.0–3.0 mmol/L** and Z3 at 3–5 mmol/L (Zones page, calculator). Half of Z2 and part of Z3 are on the wrong side of the reference bands, so the "close to Hellard" verdicts are built on mismatched bins. | zoneReading appends an honest mapping caveat whenever it compares to Hellard: 'this site's Z2 runs to about 3 mmol/L and Z4 starts near 5, so expect a larger Z1–Z2 share than Hellard's below-2-mmol figure and treat the comparison as approximate.' Verified with a synthetic 4-week log (restored afterwards). | fixed |
| R2-DOMAIN-13 | season (Cuts) | low | stated as fact without a source. In the current structure the Toyota National Championships is the Worlds/Olympic-team selection meet and carries the fastest domestic standards; the December U.S. Open is the more attainable senior tier (its 2026 standards were published unchanged from 2025). The page otherwise carefully avoids ranking claims. Could not read the 2026 standards PDFs in this environment, so verify against the two linked PDFs before rewording. | Verified against the official 2026 PDFs (18&U tables, LCM): Nationals men's 100 FR 51.09 vs U.S. Open 51.99, women's 56.69 vs 57.69, 50 FR 23.19 vs 23.79 — Nationals cuts are faster, so the claim was reversed. Card now: 'Nationals (summer) is the selection meet and carries the fastest domestic cuts; the U.S. Open (December) is the next tier — both standards are on usaswimming.org.' | fixed |
| R2-DOMAIN-14 | videos (Race Analysis) | low | Popovici's 46.86 (Rome 2022) has not been the world record since Pan Zhanle's 46.40 (2024); a knowledgeable reader will flag the caption. The video (id 3oZo0I4oLfU, "World-Record Freestyle \| David Popovici Technique Analysis") is correctly linked. | Caption: 'Technique analysis of David Popovici's 2022 world-record 100m freestyle (46.86, since beaten).' Shipped as "…2022 world-record 100 m freestyle (46.86, since beaten)". | fixed |
| R2-DOMAIN-15 | visualizer (mannequin fallback + AI fallback motions) | low | (a) the dolphin "Peak Streamline" record (shown when viz-human.js fails to load) now carries vel 2.05 after R1-DOMAIN-10 but its description still says velocity "peaks around 2.8–3.2 m/s". (b) The built-in "Breaststroke Pullout" reconstruction (FALLBACK_MOTIONS) calls the underwater pull "the fastest point in the entire stroke" at 2.1 m/s while its own push-off phase reads 2.4 m/s, and its five phases contain no breaststroke kick (knees stay ≤ 38°) although the Technique page's legal sequence is pull → recover → breaststroke kick → breakout. | Fallback dolphin 'Peak Streamline' desc: 'Push-off velocity of 2.5–3.0 m/s decays through the glide — start kicking before it drops below swimming speed'. FALLBACK_MOTIONS pullout: pull desc 'the fastest swum part of the stroke, riding the wall's speed'; new phase 'Breaststroke Kick to Breakout' (hi 35, kn 120 → 10, arms extending) inserted before 'Breakout Reach', velocities 2.4 → 2.1 → 1.9 → 1.5 → 1.3 → 1.75. Verified on the human rig: 6 phases load, phase bar shows 6 steps, the knee drops to y 0.27 at the heel-draw. | fixed |
| R2-DOMAIN-16 | visualizer (Ask the Visualizer) | low | the "Propulsion Mechanics" prompt frames propulsion as Bernoulli lift versus Newtonian drag. The Bernoulli explanation of swimming propulsion has been set aside since the 1990s (Toussaint, Sanders; drag- and vortex-based lift, not Bernoulli pressure differences); asking the model to teach it invites a dated answer. | VIZ_CONCEPTS.propulsion: 'drag-based and lift-based propulsion (vortex shedding and unsteady flow, not the dated Bernoulli explanation), the forearm-hand paddle, why an early vertical forearm determines efficiency'. | fixed |
| R2-DOMAIN-17 | planner (Race Predictor reference) | low | the caption says every typical exponent is "the mean of the men's and women's long-course world-record ratios for each step", but there is no long-course 100 IM (it is a short-course-only event), so the IM 100→200 exponent cannot come from that source; the 100 IM is also offered in the predictor only for SCY/SCM courses while the exponent is course-agnostic. | Checked the 25 m records: 108.88/49.28 and 121.63/55.11 both give b ≈ 1.14, so the value stands; the IM 100 → 200 row now carries an * (abbr title 'Derived from the 25 m-pool records — there is no 50 m-pool 100 IM') and the caption footnotes it. | fixed |
| R2-DOMAIN-18 | planner (Split Planner) | low | with strategy "Negative split" and the default −2 % fade, a 100 (2 lengths LCM) shows "2nd vs 1st **+2.6 %**" because the start bonus is subtracted from the first length after the ramp (verified: 100 BR 1:00 → 29.61 / 30.39). The label says negative, the number says positive, and only the small caption explains that the fade "applies to swum lengths". | buildSplits returns halfDiffSwumPct (start bonus added back to the first half); the halves box shows '2nd vs 1st (as swum) +2.6%' and '2nd vs 1st (excl. dive) −2.0%' whenever the bonus applies and the two differ; splitsAsText prints '+2.6% as swum (-2.0% excluding the dive)'. Verified: 100 BR 1:00 neg −2% → +2.63 / −2.00. | fixed |

**Round 2 totals:** 120 findings — 120 fixed, 0 partial, 0 deferred, 0 not reproduced. Post-merge fix commits: "forensic r2: post-merge fix" (single Enter-to-submit handler) and "forensic r2: post-merge fix (viz-human.js notes string)".
